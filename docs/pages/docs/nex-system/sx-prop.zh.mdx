## sx 属性

Nex System 的核心是 `sx` prop，它为您提供了一种快速有效的方法，直接将预设的 token 应用于 React 元素。

这个属性提供了一个 CSS 的超集，除包含所有 CSS 属性和选择器之外，还有 Nex System 内置的自定义属性和选择器。
而 CSS 属性值可以直接使用预定义的主题。如果您不满足于已有的 CSS 自定义属性和选择器以及主题，Nex System 还提供了扩展能力。
