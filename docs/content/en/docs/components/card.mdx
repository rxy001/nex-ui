---
title: Card
description: Cards contain content and actions about a single subject.
---

import { card } from '@/content/_demo'

# Card

Cards contain content and actions about a single subject.

<ComponentLinks component='card' />

---

## Usage

<Example {...card.usage} />

### Composition

You can use other NexUI components inside the Card to compose a more complex Card.

<Example {...card.composition} />

### With Image

<Example {...card.withImage} />

### Primary Action

Often, a Card allows users to interact with the entirety of its surface to trigger its main action.
The action area of the Card can be specified by wrapping its contents in a `CardActionArea` component.

<Example {...card.primaryAction} />

### Hover Effect

Set the `hoverable` prop to `true` to enable the hover effect, giving the Card visual feedback on hover.

<Example {...card.hoverEffect} />

### Blurred Card

Set the `blurred` prop to `true` to enable the blur effect on the Card.

<Example {...card.blurredCard} />

## API

### Card Props

Card inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'Usually, CardHeader, CardBody, CardFooter, and other custom content.',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'none'"],
      default: "'md'",
      description: 'The border radius of the Card.',
    },
    {
      prop: 'shadow',
      type: ["'xs'", "'sm'", "'md'", "'lg'", "'xl'"],
      default: "'md'",
      description: 'The shadow of the Card.',
    },
    {
      prop: 'blurred',
      type: 'boolean',
      description: 'If true, applies a backdrop filter to the Card.',
    },
    {
      prop: 'hoverable',
      type: 'boolean',
      description: 'If true, the Card will have a hover effect.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### CardHeader Props

CardHeader inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The content of the CardHeader, which disables the avatar, action, title, and subtitle props.',
    },
    {
      prop: 'title',
      type: 'ReactNode',
      description: 'The title of the CardHeader.',
    },
    {
      prop: 'subtitle',
      type: 'ReactNode',
      description: 'The subtitle of the CardHeader.',
    },
    {
      prop: 'avatar',
      type: 'ReactNode',
      description: 'The avatar of the CardHeader.',
    },
    {
      prop: 'action',
      type: 'ReactNode',
      description: 'The action area of the CardHeader.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### CardHeader Slots

<SlotsTable
  dataSource={[
    {
      slot: 'content',
      class: '.nui-card-header-content',
      component: "'div'",
      description: 'The content container of the CardHeader.',
    },
    {
      slot: 'title',
      class: '.nui-card-header-title',
      component: "'div'",
      description: 'The title container of the CardHeader.',
    },
    {
      slot: 'subtitle',
      class: '.nui-card-header-subtitle',
      component: "'div'",
      description: 'The subtitle container of the CardHeader.',
    },
  ]}
/>

### CardBody Props

CardBody inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the CardBody.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### CardFooter Props

CardFooter inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the CardFooter.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>
