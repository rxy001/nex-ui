---
title: Dialog
description: Display a dialog with custom content that requires attention or provides additional information.
---

import { dialog } from '@/content/_demo'

# Dialog

Dialog displays custom content that requires attention or provides additional information.

<ComponentLinks component='dialog' ariaCompatible />

## Usage

<Example {...dialog.usage} />

### Hide Backdrop

Set the `hideBackdrop` property of Dialog to `true` to hide the Dialog background overlay.

<Example {...dialog.hideBackdrop} />

### Prevent Scroll

Set the `preventScroll` property of Dialog to `true` to prevent page scrolling when the Dialog is open.

<Example {...dialog.preventScroll} />

### Keep Mounted

Set the `keepMounted` property of Dialog to `true` to keep the Dialog content mounted when closed.

<Example {...dialog.keepMounted} />

### Sizes

Use the `size` property of DialogContent to set the maximum width of the Dialog. Defaults to `md`.

<Example {...dialog.sizes} />

### FullScreen

Set the `fullScreen` property of DialogContent to `true` to make the Dialog full-screen.

<Example {...dialog.fullScreen} />

### Placements

Use the `placement` property of DialogContent to set the display position of the Dialog. Defaults to `top`.

<Example {...dialog.placements} />

### Custom Motion

Use the `motionProps` property of DialogContent to customize the animation effects for the `visible` and `hidden` states.

<Example {...dialog.customMotion} />

### Overflow Scroll

Use the `scroll` property of DialogContent to control the scrolling behavior of Dialog content.

- `inside`: Content scrolls inside the Dialog.
- `outside`: Content scrolls outside the Dialog.

<Example {...dialog.scrollBehavior} />

### Asynchronously Close

DialogClose supports asynchronously closing the Dialog. The `onClick` event of its `children` can return a `Promise`, and the Dialog will close when the `Promise` is resolved.

<Example {...dialog.asynchronouslyClose} />

### Controlled

Use the `open` and `onOpenChange` props of Dialog to control the open state of the Dialog.

<Example {...dialog.controlled} />

## API

### Dialog Props

Dialog inherits the props of `as` (by default, `m.div`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Usually, DialogContent, DialogTrigger, etc.',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: 'If true, the Dialog is shown. (controlled)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'If true, the Dialog is shown by default. (uncontrolled)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description:
        'Handler that is called when the Dialog is opened or closed.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'hideBackdrop',
      type: 'boolean',
      description: 'If true, the backdrop is not rendered.',
      default: 'false',
    },
    {
      prop: 'container',
      type: ['HTMLElement', 'null', '() => HTMLElement | null'],
      description: 'The container element for the portal.',
      default: 'document.body',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      description: 'If true, always keep the children in the DOM.',
      default: 'false',
    },
    {
      prop: 'preventScroll',
      type: 'boolean',
      description: 'If true, the Dialog prevents page scrolling.',
      default: 'false',
    },
    {
      prop: 'closeOnInteractBackdrop',
      type: 'boolean',
      description: 'If true, closes the Dialog when the backdrop is clicked.',
      default: 'true',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      description: 'If true, closes the Dialog when the Escape key is pressed.',
      default: 'true',
    },
    {
      prop: 'restoreFocus',
      type: 'boolean',
      description:
        'If true, the Dialog restores focus to the previously focused element once the Dialog is hidden or unmounted.',
      default: 'true',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description:
        'Callback function that is called when the Dialog is closed.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: 'm.div',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### Dialog Slots

<SlotsTable
  dataSource={[
    {
      slot: 'backdrop',
      class: '.nui-dialog-backdrop',
      component: "'div'",
      description: 'The backdrop element of the Dialog.',
    },
  ]}
/>

### DialogContent Props

DialogContent inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Usually, DialogHeader, DialogBody, DialogFooter, etc.',
    },
    {
      prop: 'closeIcon',
      type: 'ReactNode',
      description: 'Custom close button to display on top right corner.',
    },
    {
      prop: 'size',
      type: ["'xs'", "'sm'", "'md'", "'lg'", "'xl'", "'full'"],
      description: 'The size of the Dialog.',
      default: "'md'",
    },
    {
      prop: 'placement',
      type: ["'top'", "'bottom'", "'left'", "'right'"],
      description: 'The display position of the Dialog.',
      default: "'top'",
    },
    {
      prop: 'scroll',
      type: ["'inside'", "'outside'"],
      description: 'The Dialog scroll behavior.',
      default: "'outside'",
    },
    {
      prop: 'fullScreen',
      type: 'boolean',
      description: 'If true, the Dialog is full-screen.',
      default: 'false',
    },
    {
      prop: 'hideCloseButton',
      type: 'boolean',
      description: 'If true, the close button is not rendered.',
      default: 'false',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'motionProps',
      type: ['HTMLMotionProps', '(placement) => HTMLMotionProps'],
      description: 'The props to modify the framer motion animation.',
    },
    {
      prop: 'aria-labelledby',
      type: 'string',
      description: 'The id(s) of the element(s) that label the DialogContent.',
    },
    {
      prop: 'aria-describedby',
      type: 'string',
      description:
        'The id(s) of the element(s) that describe the DialogContent.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DialogContent Slots

<SlotsTable
  dataSource={[
    {
      slot: 'closeButton',
      class: '.nui-dialog-content-close-button',
      component: "'button'",
      description: 'The close button element of the DialogContent.',
    },
    {
      slot: 'paper',
      class: '.nui-dialog-content-paper',
      component: 'm.section',
      description: 'The paper element of the DialogContent.',
    },
  ]}
/>

### DialogHeader Props

DialogHeader inherits the props of `as` (by default, `'h2'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the DialogHeader.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'h2'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DialogBody Props

DialogBody inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the DialogBody.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DialogFooter Props

DialogFooter inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the DialogFooter.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DialogTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The element that triggers the Dialog to open; must support the `onClick` event.',
    },
  ]}
/>

### DialogClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The element that triggers the Dialog to close; must support the `onClick` event.',
    },
  ]}
/>
