---
title: Accordion
description: The accordion component allows expanding and collapsing content sections on the page.
---

import { Accordion } from '@nex-ui/react'
import { accordion } from '@/content/_demo'

# Accordion

The accordion component enables expanding and collapsing related content sections on the page.

<ComponentLinks component='accordion' ariaCompatible />

---

## Usage

<Example {...accordion.usage} />

### Disabled

<Example {...accordion.disabled} />

### Expand Multiple Items

Set the `multiple` prop to `true` to allow expanding multiple items simultaneously.

<Example {...accordion.expandMultiple} />

### Default Expanded Keys

Use the `defaultExpandedKeys` prop to set initially expanded items by default.

<Example {...accordion.defaultExpandedKeys} />

### Variants

The accordion component provides multiple visual variants through the `variant` prop.

#### Outlined

<Example {...accordion.outlinedVariant} />

#### Underlined

<Example {...accordion.underlinedVariant} />

### Disabled Keys

Control disabled items using the `disabledKeys` prop.

<Example {...accordion.disabledKeys} />

### Custom Indicator

Customize the expand/collapse indicator using the `indicator` prop. Use `hideIndicator` to completely hide the indicator.

<Example {...accordion.indicator} />

### Controlled

The accordion supports controlled mode through the `expandedKeys` prop.

<Example {...accordion.controlled} />

## API

### Accordion Props

The accordion props inherit the props of `as` (by default, it is the `div` tag).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The contents of the collection. Usually the array of AccordionItem.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Class name for the root element.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root element.',
    },
    {
      prop: 'multiple',
      type: 'boolean',
      default: 'false',
      description:
        'If true, the accordion items can be expanded at the same time.',
    },
    {
      prop: 'defaultExpandedKeys',
      type: 'Key[]',
      description:
        'The default expanded keys in the collection (uncontrolled).',
    },
    {
      prop: 'expandedKeys',
      type: 'Key[]',
      description:
        'The currently expanded keys in the collection (controlled).',
    },
    {
      prop: 'onExpandedKeysChange',
      type: '(keys: Key[]) => void',
      description: 'Handler that is called when the expanded keys change.',
    },
    {
      prop: 'disabledKeys',
      type: 'Key[]',
      description: 'The item keys that are disabled.',
    },
    {
      prop: 'MotionProps',
      type: 'HTMLMotionProps',
      description: 'The motion properties of the accordion.',
    },
    {
      prop: 'indicator',
      type: 'ReactNode',
      description:
        'The accordion item expanded indicator, usually an arrow icon.',
    },
    {
      prop: 'hideIndicator',
      type: 'boolean',
      description: 'If true, the accordion items indicator is hidden.',
    },
    {
      prop: 'indicatorMotionProps',
      type: 'HTMLMotionProps',
      description: 'The motion properties of the accordion indicator.',
    },
    {
      prop: 'variant',
      type: ["'outlined'", "'underlined'"],
      default: "'underlined'",
      description: 'The accordion appearance style.',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'If true, the accordion items are disabled',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      default: 'true',
      description:
        'If true, the accordion item content should always be mounted.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'sx',
      type: 'SxProps<AccordionOwnerState>',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
  ]}
/>

### AccordionItem Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the component.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root element.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root element.',
    },
    {
      prop: 'indicator',
      type: 'ReactNode',
      description: 'The accordion item expanded indicator.',
    },
    {
      prop: 'hideIndicator',
      type: 'boolean',
      description: 'If true, the accordion item indicator is hidden.',
    },
    {
      prop: 'indicatorMotionProps',
      type: 'HTMLMotionProps',
      description: 'The props to modify the framer-motion animation. Use the variants API to create your own animation.',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      default: 'false',
      description: ' If true, the accordion item is disabled.',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      default: 'true',
      description: 'If true, the accordion item content should always be mounted.',
    },
    {
      prop: 'motionProps',
      type: 'HTMLMotionProps',
      description: 'The props to modify the framer-motion animation. Use the variants API to create your own animation.',
    },
    {
      prop: 'itemKey',
      type: 'Key',
      description: 'The accordion item key.',
    },
    {
      prop: 'title',
      type: 'ReactNode',
      description: 'The accordion item title.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description:
        'The component or element to render as the root.',
    },
    {
      prop: 'sx',
      type: 'SxProps<AccordionItemOwnerState>',
      description: 'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classes',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },

]}
/>

### AccordionItem Slots

The accordionItem props inherit the props of `as` (by default, it is the `div` tag).

<SlotsTable
  dataSource={[
    {
      slot: 'heading',
      class: '.nui-accordion-item-heading',
      component: "'h3'",
      description: 'Heading element for the item.',
    },
    {
      slot: 'content',
      class: '.nui-accordion-item-panel',
      component: "'span'",
      description: 'Content panel element.',
    },
    {
      slot: 'indicator',
      class: '.nui-accordion-item-indicator',
      component: "'m.span'",
      description: 'Indicator container element.',
    },
    {
      slot: 'trigger',
      class: '.nui-accordion-item-trigger',
      component: "'button'",
      description: 'Toggle button element.',
    },
  ]}
/>
