---
title: Drawer
description: Drawer is a component that slides in from the side of the screen.
---

import { drawer } from '@/content/_demo'

# Drawer

Drawer is a component that slides in from the side of the screen.

<ComponentLinks component='drawer' />

## Usage

<Example {...drawer.usage} />

### Hide Backdrop

Set the `hideBackdrop` property of Drawer to `true` to hide the Drawer background overlay.

<Example {...drawer.hideBackdrop} />

### Prevent Scroll

Set the `preventScroll` property of Drawer to `true` to prevent page scrolling when the Drawer is open.

<Example {...drawer.preventScroll} />

### Keep Mounted

Set the `keepMounted` property of Drawer to `true` to keep the Drawer content mounted when closed.

<Example {...drawer.keepMounted} />

### Sizes

Use the `size` property of DrawerContent to set the size of the Drawer. Defaults to `md`.

<Example {...drawer.sizes} />

### Placements

Use the `placement` property of DrawerContent to set the display position of the Drawer. Defaults to `right`.

<Example {...drawer.placements} />

### Custom Motion

Use the `motionProps` property of DrawerContent to customize the animation effects for showing/hiding the Drawer.

<Example {...drawer.customMotion} />

### Asynchronously Close

DrawerClose supports asynchronously closing the Drawer. The `onClick` event of its `children` can return a `Promise`, and the Drawer will close after the `Promise` is resolved.

<Example {...drawer.asynchronouslyClose} />

### Controlled Drawer

Use the `open` and `onOpenChange` props of Drawer to control the open state of the Drawer.

<Example {...drawer.controlled} />

## API

### Drawer Props

Drawer inherits the props of `as` (by default, `m.div`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Usually, DrawerContent, DrawerTrigger, etc.',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: 'If true, the Drawer is shown. (controlled)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'If true, the Drawer is shown by default. (uncontrolled)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description:
        'Handler that is called when the Drawer is opened or closed.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'hideBackdrop',
      type: 'boolean',
      description: 'If true, the backdrop is not rendered.',
      default: 'false',
    },
    {
      prop: 'container',
      type: ['HTMLElement', 'null', '() => HTMLElement | null'],
      description: 'The container element for the portal.',
      default: 'document.body',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      description: 'If true, always keep the children in the DOM.',
      default: 'false',
    },
    {
      prop: 'preventScroll',
      type: 'boolean',
      description: 'If true, the Drawer prevents page scrolling.',
      default: 'false',
    },
    {
      prop: 'closeOnInteractBackdrop',
      type: 'boolean',
      description: 'If true, closes the Drawer when the backdrop is clicked.',
      default: 'true',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      description: 'If true, closes the Drawer when the Escape key is pressed.',
      default: 'true',
    },
    {
      prop: 'restoreFocus',
      type: 'boolean',
      description:
        'If true, the Drawer restores focus to the previously focused element once the Drawer is hidden or unmounted.',
      default: 'true',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description:
        'Callback function that is called when the Drawer is closed.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: 'm.div',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### Drawer Slots

<SlotsTable
  dataSource={[
    {
      slot: 'backdrop',
      class: '.nui-drawer-backdrop',
      component: "'div'",
      description: 'The backdrop element of the Drawer.',
    },
  ]}
/>

### DrawerContent Props

DrawerContent inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Usually, DrawerHeader, DrawerBody, DrawerFooter, etc.',
    },
    {
      prop: 'closeIcon',
      type: 'ReactNode',
      description: 'Custom close button to display on the top-right corner.',
    },
    {
      prop: 'size',
      type: ["'xs'", "'sm'", "'md'", "'lg'", "'xl'", "'full'"],
      description: 'The size of the Drawer.',
      default: "'md'",
    },
    {
      prop: 'placement',
      type: ["'top'", "'bottom'", "'left'", "'right'"],
      description: 'The display position of the Drawer.',
      default: "'right'",
    },
    {
      prop: 'hideCloseButton',
      type: 'boolean',
      description: 'If true, the close button is not rendered.',
      default: 'false',
    },
    {
      prop: 'motionProps',
      type: ['HTMLMotionProps', '(placement) => HTMLMotionProps'],
      description: 'The props to modify the framer motion animation.',
    },
    {
      prop: 'aria-labelledby',
      type: 'string',
      description: 'The id(s) of the element(s) that label the DrawerContent.',
    },
    {
      prop: 'aria-describedby',
      type: 'string',
      description:
        'The id(s) of the element(s) that describe the DrawerContent.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DrawerContent Slots

<SlotsTable
  dataSource={[
    {
      slot: 'closeButton',
      class: '.nui-drawer-content-close-button',
      component: "'button'",
      description: 'The close button element of the DrawerContent.',
    },
    {
      slot: 'paper',
      class: '.nui-drawer-content-paper',
      component: 'm.section',
      description: 'The paper element of the DrawerContent.',
    },
  ]}
/>

### DrawerHeader Props

DrawerHeader inherits the props of `as` (by default, `'h2'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the DrawerHeader.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'h2'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DrawerBody Props

DrawerBody inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the DrawerBody.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DrawerFooter Props

DrawerFooter inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the DrawerFooter.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
  ]}
/>

### DrawerTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The element that triggers the Drawer to open; must support the `onClick` event.',
    },
  ]}
/>

### DrawerClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The element that triggers the Drawer to close; must support the `onClick` event.',
    },
  ]}
/>
