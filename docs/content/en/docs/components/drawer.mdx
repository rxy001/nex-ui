---
title: Drawer
description: Drawer is a component that slides in from the side of the screen.
---

import { drawer } from '@/content/_demo'

# Drawer

Drawer is a component that slides in from the side of the screen.

## Usage

<Example {...drawer.usage} />

### Hide Backdrop

Set the `hideBackdrop` property of Drawer to `true` to hide the background overlay.

<Example {...drawer.hideBackdrop} />

### Prevent Scroll

Set the `preventScroll` property of Drawer to `true` to prevent page scrolling when the drawer is open.

<Example {...drawer.preventScroll} />

### Keep Mounted

Set the `keepMounted` property of Drawer to `true` to keep the drawer content mounted when closed.

<Example {...drawer.keepMounted} />

### Sizes

Use the `size` property of DrawerContent to set the size of the drawer, defaults to `md`.

<Example {...drawer.sizes} />

### Placements

Use the `placement` property of DrawerContent to set the display position of the drawer, defaults to `right`.

<Example {...drawer.placements} />

### Custom Motion

Use the `motionProps` property of DrawerContent to customize the animation effects for showing/hiding the drawer.

<Example {...drawer.customMotion} />

### Asynchronously Close

DrawerClose supports asynchronously closing the drawer. The `onClick` event of its `children` can return a `Promise`, and the drawer will close after the `Promise` is resolved.

<Example {...drawer.asynchronouslyClose} />

### Controlled Drawer

Use the `open` and `onOpenChange` props of Drawer to control the drawer's open state.

<Example {...drawer.controlled} />

## API

### Drawer Props

Drawer inherit the props of `as` (by default, `m.div`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: "It's usually DrawerContent, DrawerTrigger, etc.",
    },
    {
      prop: 'open',
      type: 'boolean',
      description: 'If true, the drawer is shown.(controlled)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'If true, the drawer is shown by default.(uncontrolled)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description: 'Handler that is called when the drawer is opened or closed',
    },
    {
      prop: 'className',
      type: 'string',
      description: 'Additional class names to apply to the root element.',
    },
    {
      prop: 'hideBackdrop',
      type: 'boolean',
      description: 'If true, the backdrop is not rendered.',
      default: 'false',
    },
    {
      prop: 'container',
      type: ['HTMLElement', 'null', '() => HTMLElement | null'],
      description: 'The container element for the portal.',
      default: 'document.body',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      description: 'If true, always keep the children in the DOM.',
      default: 'false',
    },
    {
      prop: 'preventScroll',
      type: 'boolean',
      description: 'If true, the drawer prevents page scrolling.',
      default: 'false',
    },
    {
      prop: 'closeOnInteractBackdrop',
      type: 'boolean',
      description: 'If true, close the drawer when the backdrop is clicked.',
      default: 'true',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      description: 'If true, close the modal when the escape key is pressed.',
      default: 'true',
    },
    {
      prop: 'restoreFocus',
      type: 'boolean',
      description:
        'If true, the drawer will restore focus to previously focused element once the modal is hidden or unmounted.',
      default: 'true',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description:
        'Callback function that is called when the drawer is closed.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: 'm.div',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classes',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
  ]}
/>

### Drawer Slots

<SlotsTable
  dataSource={[
    {
      slot: 'backdrop',
      class: '.nui-drawer-backdrop',
      component: "'div'",
      description: 'The backdrop element of the drawer.',
    },
  ]}
/>

### DrawerContent Props

DrawerContent inherit the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: "It's usually DrawerHeader, DrawerBody, DrawerFooter, etc.",
    },
    {
      prop: 'closeIcon',
      type: 'ReactNode',
      description: 'Custom close button to display on top right corner.',
    },
    {
      prop: 'size',
      type: ["'xs'", "'sm'", "'md'", "'lg'", "'xl'", "'full'"],
      description: 'The size of the drawer.',
      default: "'md'",
    },
    {
      prop: 'placement',
      type: ["'top'", "'bottom'", "'left'", "'right'"],
      description: 'The display position of the drawer.',
      default: "'right'",
    },
    {
      prop: 'hideCloseButton',
      type: 'boolean',
      description: 'If true, the close button is not rendered.',
      default: 'false',
    },
    {
      prop: 'motionProps',
      type: ['HTMLMotionProps', '(placement) => HTMLMotionProps'],
      description: 'The props to modify the framer motion animation.',
    },
    {
      prop: 'aria-labelledby',
      type: 'string',
      description: 'The id(s) of the element(s) that label the modal.',
    },
    {
      prop: 'aria-describedby',
      type: 'string',
      description: 'The id(s) of the element(s) that describe the modal.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classes',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
  ]}
/>

### DrawerContent Slots

<SlotsTable
  dataSource={[
    {
      slot: 'closeButton',
      class: '.nui-drawer-content-close-button',
      component: "'button'",
      description: 'The close button element of the drawer content.',
    },
    {
      slot: 'paper',
      class: '.nui-drawer-content-paper',
      component: 'm.section',
      description: 'The paper element of the drawer.',
    },
  ]}
/>

### DrawerHeader Props

DrawerHeader inherit the props of `as` (by default, `'h2'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the drawer header.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'h2'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
  ]}
/>

### DrawerBody Props

DrawerBody inherit the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the drawer body.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
  ]}
/>

### DrawerFooter Props

DrawerFooter inherit the props of `as` (by default,`'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the drawer footer.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description: 'The component or element to render as the root.',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
  ]}
/>

### DrawerTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The element that triggers the drawer to open, must support `onClick` event.',
    },
  ]}
/>

### DrawerClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The element that triggers the drawer to close, must support `onClick` event.',
    },
  ]}
/>
