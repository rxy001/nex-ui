---
title: RadioGroup
description: RadioGroup 允许用户从一组中选择一个项目。
---

import { radioGroup } from '@/content/_demo'

# RadioGroup

RadioGroup allow users to select a single option from a list of mutually exclusive options.

## Usage

<Example {...radioGroup.usage} />

### Disabled

<Example {...radioGroup.disabled} />

### Colors

<Example {...radioGroup.colors} />

### Sizes

Use the `size` prop to set the size of the RadioGroup, default is `md`.

<Example {...radioGroup.sizes} />

### Default Value

Use the `defaultValue` prop to set the default selected option of the RadioGroup.

<Example {...radioGroup.defaultValue} />

### Vertical

Use the `orientation` prop to set the layout direction of the RadioGroup, default is `horizontal`.

<Example {...radioGroup.vertical} />

### Controlled

Use the `value` and `onValueChange` props to control the radio input value.

<Example {...radioGroup.controlled} />

## API

### Radio Props

Radio inherit the props of `as` (by default, `'input'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The label of the radio.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'ryan'",
        "'red'",
        "'green'",
        "'pick'",
        "'purple'",
        "'gray'",
      ],
      default: "'blue'",
      description: 'The color of the radio.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      default: "'md'",
      description: 'The size of the radio.',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'If true, the radio is disabled.',
    },
    {
      prop: 'checked',
      type: 'boolean',
      description: 'If true, the radio is checked.(controlled)',

    },
    {
      prop: 'defaultChecked',
      type: 'boolean',
      description: 'If true, the radio is checked by default.(uncontrolled)',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root element.',
      },
    {
      prop: 'name',
      type: 'string',
      description: 'The name of the radio.',
    },
    {
      prop: 'value',
      type: ['string', 'number'],
      description: 'The value of the radio.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the input element inside the radio.',
    },
    {
      prop: 'onChange',
      type: '(event: ChangeEvent<HTMLInputElement>) => void',
      description:
        "Handler that is called when the element's checked state changes.",
    },
    {
      prop: 'onCheckedChange',
      type: '(checked: boolean) => void',
       description:
        "Handler that is called when the element's checked state changes.",
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'input'",
      description: 'The component used for the input element.',
    },
    {
      prop: 'sx',
      type: 'SxProp<RadioOwnerState>',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classes',
      type: "Partial<Record<'root' | 'input' | 'dot' | 'label', ClassValue>>",
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },

]}
/>

### Radio Slots

<SlotsTable
  dataSource={[
    {
      slot: 'root',
      class: '.nui-radio-root',
      component: "'label'",
      description: 'The root element of the radio.',
    },
    {
      slot: 'dot',
      class: '.nui-radio-dot',
      component: "'span'",
      description: 'The container of the circle inside the checkbox.',
    },
    {
      slot: 'label',
      class: '.nui-radio-label',
      component: "'span'",
      description: 'The label element of the radio.',
    },

]}
/>

### RadioGroup Props

CheckboxGroup inherit the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The list of radio elements.',
    },
    {
      prop: 'label',
      type: 'ReactNode',
      description: 'The label of the radio group.',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      description: 'If true, the radios are disabled.',
    },
    {
      prop: 'name',
      type: 'string',
      description:
        'The name of the radio group, used when submitting an HTML form.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'ryan'",
        "'red'",
        "'green'",
        "'pick'",
        "'purple'",
        "'gray'",
      ],
      description: 'The color of the radios',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      description: 'The size of the radios',
    },
    {
      prop: 'value',
      type: ['T extends string | number'],
      description: 'The current selected values. (controlled)',
    },
    {
      prop: 'defaultValue',
      type: ['T extends string | number'],
      description: 'The default checked values. (uncontrolled)',
    },
    {
      prop: 'onValueChange',
      type: '(value: T) => void',
      description: 'Handler that is called when the value changes.',
    },
    {
      prop: 'orientation',
      type: ["'horizontal'", "'vertical'"],
      default: "'horizontal'",
      description: 'The axis the radio group items should align with.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component used for the root element.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root element.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root element.',
    },
    {
      prop: 'sx',
      type: 'SxProp<RadioGroupOwnerState>',
      description: 
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classes',
      type: "Partial<Record<'root' | 'label' | 'wrapper', ClassValue>>",
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },

]}
/>

### RadioGroup Slots

<SlotsTable
  dataSource={[
    {
      slot: 'label',
      class: '.nui-radio-group-label',
      component: "'h3'",
      description: 'The label element of the radio group.',
    },
    {
      slot: 'wrapper',
      class: '.nui-radio-group-wrapper',
      component: "'div'",
      description: 'The container for all radios in the radio group.',
    },
  ]}
/>
