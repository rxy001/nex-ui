---
title: Popover
description: Popover is a non-modal dialog that floats around its trigger element. It is commonly used to display additional rich content on top of an element.
---

import { popover } from '@/content/_demo'

# Popover

Popover is a non-modal dialog that floats around its trigger element. It is commonly used to display additional rich content on top of an element.

<ComponentLinks component='popover' />

---

## Anatomy

NexUI provides four components for building popovers:

- `Popover`: The root component that manages Popover state.
- `PopoverTrigger`: The component used to open the Popover.
- `PopoverContent`: The component that displays Popover content.
- `PopoverClose`: The component used to close the Popover.

```tsx
import {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverClose,
} from '@nex-ui/react'
```

## Usage

<Example {...popover.usage} />

### Colors

<Example {...popover.colors} />

### Placements

<Example {...popover.placements} />

### Offset

Use the `offset` prop on `PopoverContent` to set main-axis and cross-axis offsets. Set it to `false` to disable offset.

<Example {...popover.offset} />

### Flip

Use the `flip` prop on `PopoverContent` to adjust placement and keep the Popover in view. The default is `{ mainAxis: true, crossAxis: true }`. Set it to `false` to disable flipping.

<Example {...popover.flip} />

### Shift

Use the `shift` prop on `PopoverContent` to keep the Popover in view. Defaults to `true`.

<Example {...popover.shift} />

### With Form

Popover manages focus within its content, so you can safely use it with form elements. When the Popover closes, focus returns to the trigger element.

<Example {...popover.withForm} />

### Controlled

<Example {...popover.controlled} />

## API

### Popover Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Usually, PopoverTrigger and PopoverContent components.',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: 'If true, opens the Popover. (controlled)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'If true, opens the Popover by default. (uncontrolled)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description: 'Callback fired when the Popover opens or closes.',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description: 'Callback fired when the Popover closes.',
    },
  ]}
/>

### PopoverContent Props

PopoverContent inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the Popover.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the Popover root element.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'The className for the Popover root element.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description:
        'The component used for the Popover root element. It can be an HTML element string or a custom component.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: 'The sx prop for the Popover root element.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'classNames for each Popover slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'slotProps for each Popover slot.',
    },
    {
      prop: 'color',
      type: [
        `'default'`,
        `'blue'`,
        `'orange'`,
        `'yellow'`,
        `'cyan'`,
        `'red'`,
        `'green'`,
        `'pink'`,
        `'purple'`,
        `'gray'`,
      ],
      default: `'default'`,
      description: 'The color of the Popover.',
    },
    {
      prop: 'maxWidth',
      type: ['string', 'number'],
      description: 'The maximum width of the Popover.',
    },
    {
      prop: 'width',
      type: ['string', 'number'],
      default: 'auto',
      description: 'The width of the Popover.',
    },
    {
      prop: 'radius',
      type: [`'none'`, `'sm'`, `'md'`, `'lg'`, `'full'`],
      default: `'md'`,
      description: 'The border radius of the Popover.',
    },
    {
      prop: 'container',
      type: ['HTMLElement | null', '() => HTMLElement | null'],
      description: 'The container element for the portal.',
      default: 'document.body',
    },
    {
      prop: 'placement',
      type: [
        `'top'`,
        `'top-start'`,
        `'top-end'`,
        `'bottom'`,
        `'bottom-start'`,
        `'bottom-end'`,
        `'left'`,
        `'left-start'`,
        `'left-end'`,
        `'right'`,
        `'right-start'`,
        `'right-end'`,
      ],
      default: `'bottom'`,
      description: 'The placement of the Popover.',
    },
    {
      prop: 'motionProps',
      type: 'HTMLMotionProps',
      description: 'Props used to customize Popover animations.',
    },
    {
      prop: 'offset',
      type: ['false', 'number', '{ mainAxis?: number; crossAxis?: number }'],
      default: '5',
      description: 'Sets the Popover offset, or disables offset.',
    },
    {
      prop: 'flip',
      type: ['false', '{ mainAxis?: boolean; crossAxis?: boolean }'],
      default: '{ mainAxis: true, crossAxis: true }',
      description: 'Changes Popover placement to keep it in view.',
    },
    {
      prop: 'shift',
      type: 'boolean',
      default: 'true',
      description: 'If true, shifts the Popover to keep it in view.',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      default: 'false',
      description:
        'If true, keeps the Popover mounted in the DOM when not open.',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the Popover when the Escape key is pressed.',
    },
    {
      prop: 'closeOnDetached',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the Popover when the trigger element is fully obscured.',
    },
    {
      prop: 'disableAnimation',
      type: 'boolean',
      default: 'false',
      description: 'If true, disables the animation for the Popover.',
    },
    {
      prop: 'loopFocus',
      type: 'boolean',
      default: 'true',
      description: 'If true, traps focus within the Popover when open.',
    },
    {
      prop: 'restoreFocus',
      type: 'boolean',
      default: 'true',
      description:
        'If true, restores focus to the previously focused element when the it closes.',
    },
    {
      prop: 'autoFocus',
      type: 'boolean',
      default: 'false',
      description:
        'If true, autofocuses the first focusable element when the Popover opens.',
    },
  ]}
/>

### PopoverContent Slots

<SlotsTable
  dataSource={[
    {
      slot: 'paper',
      class: '.nui-popover-content-paper',
      component: "'div'",
      description: 'The content container of the Popover.',
    },
  ]}
/>

### PopoverTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactElement',
      description: 'The trigger element that opens the Popover when clicked.',
    },
    {
      prop: 'closeOnClick',
      type: 'boolean',
      default: 'false',
      description:
        'If true, closes the Popover when the trigger is clicked while the Popover is open.',
    },
  ]}
/>

### PopoverClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactElement',
      description: 'The trigger element that closes the Popover when clicked.',
    },
  ]}
/>
