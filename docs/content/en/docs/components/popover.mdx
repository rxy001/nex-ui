---
title: Popover
description: Popover is a non-modal dialog that floats around its disclosure. It's commonly used for displaying additional rich content on top of something.
---

import { popover } from '@/content/_demo'

# Popover

Popover is a non-modal dialog that floats around its disclosure. It's commonly used for displaying additional rich content on top of something.

<ComponentLinks component='popover' />

## Usage

<Example {...popover.usage} />

### Colors

<Example {...popover.colors} />

### Placements

<Example {...popover.placements} />

### Offset

Use the `offset` prop to set the Popover's offset, including the main axis and cross axis offsets. Setting it to `false` disables the offset.

<Example {...popover.offset} />

### Flip

Use the `flip` prop to change the placement of the popover to keep it in view. `{ mainAxis: true, crossAxis: true }` by default. Setting it to `false` disables the flip behavior.

<Example {...popover.flip} />

### Shift

Use the `shift` prop to shift the popover to keep it in view. Defaults to `true`.

<Example {...popover.shift} />

### With Form

The Popover handles the focus within the popover content. It means that you can use the popover with form elements without any problem. the focus returns to the trigger when the popover closes.

<Example {...popover.withForm} />

### Controlled

<Example {...popover.controlled} />

### Custom Motion

Use the `motionProps` prop to customize the animation effects for `visible` / `hidden`.

<Example {...popover.customMotion} />

## API

Popover inherit the props of `as` (by default, `m.div`).

### Popover Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description:
        'The content of the Popover, usually consists of PopoverTrigger and PopoverContent components.',
    },
    {
      prop: 'container',
      type: ['HTMLElement | null', '() => HTMLElement | null'],
      description:
        'The container element in which the overlay portal will be placed.',
      default: 'document.body',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: 'If true, the popover is shown. (controlled)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'If true, the popover is shown by default. (uncontrolled)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description:
        'Handler that is called when the popover is opened or closed.',
    },
    {
      prop: 'placement',
      type: [
        `'top'`,
        `'top-start'`,
        `'top-end'`,
        `'bottom'`,
        `'bottom-start'`,
        `'bottom-end'`,
        `'left'`,
        `'left-start'`,
        `'left-end'`,
        `'right'`,
        `'right-start'`,
        `'right-end'`,
      ],
      default: `'top'`,
      description:
        'The placement of the popover relative to the trigger element.',
    },
    {
      prop: 'motionProps',
      type: 'HTMLMotionProps',
      description: 'The motion properties of the popover.',
    },
    {
      prop: 'offset',
      type: ['false', 'number', '{ mainAxis?: number; crossAxis?: number }'],
      default: '5',
      description: 'Translates the popover along the specified axes.',
    },
    {
      prop: 'flip',
      type: ['false', '{ mainAxis?: boolean; crossAxis?: boolean }'],
      default: '{ mainAxis: true, crossAxis: true }',
      description: 'Changes the placement of the popover to keep it in view.',
    },
    {
      prop: 'shift',
      type: 'boolean',
      default: 'true',
      description: 'Shifts the popover to keep it in view.',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      default: 'false',
      description: 'If true, always keep the children in the DOM.',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the popover when the escape key is pressed.',
    },
    {
      prop: 'closeOnDetached',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the tooltip when the trigger element is completely obscured.',
    },
    {
      prop: 'openDelay',
      type: 'number',
      default: '0',
      description: 'The delay in milliseconds before the popover opens.',
    },
    {
      prop: 'closeDelay',
      type: 'number',
      default: '0',
      description: 'The delay in milliseconds before the popover closes.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root element.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root element.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: 'm.div',
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
  ]}
/>

### PopoverContent Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the PopoverContent component.',
    },
    {
      prop: 'color',
      type: [
        `'default'`,
        `'blue'`,
        `'orange'`,
        `'yellow'`,
        `'cyan'`,
        `'red'`,
        `'green'`,
        `'pink'`,
        `'purple'`,
        `'gray'`,
      ],
      default: `'default'`,
      description: 'The color of the PopoverContent.',
    },
    {
      prop: 'radius',
      type: [`'none'`, `'sm'`, `'md'`, `'lg'`, `'full'`],
      default: `'md'`,
      description: 'The border radius of the PopoverContent.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root element.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root element.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: 'div',
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
  ]}
/>

### PopoverTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The trigger displays the Popover when clicked.',
    },
    {
      prop: 'closeOnClick',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the tooltip when clicking the trigger element.',
    },
  ]}
/>

### PopoverClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The trigger closes the Popover when clicked.',
    },
  ]}
/>
