---
title: Divider
description: The divider is a component used to separate content on the page.
---

import { divider } from '@/content/_demo'

# Divider

The divider is a component used to separate content on the page.

<ComponentLinks component='divider' />

## Usage

<Example {...divider.usage} />

## API

### Divider Props

The divider props inherit the props of `as` (by default, it is the `div` tag).

<PropsTable
  dataSource={[
    {
      prop: 'orientation',
      description: 'The orientation of the divider.',
      type: ["'horizontal'", "'vertical'"],
      default: "'horizontal'",
    },
    {
      prop: 'className',
      description: 'Additional class names to apply to the root element.',
      type: 'ClassValue',
    },
    {
      prop: 'ref',
      description: 'Ref for forwarding to the root element.',
      type: 'Ref',
    },
    {
      prop: 'sx',
      type: 'SxProp<DividerOwnerState>',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component used for the root node.',
    },
  ]}
/>
