---
title: Divider
description: Divider is used to separate content on the page.
---

import { divider } from '@/content/_demo'

# Divider

Divider is used to separate content on the page.

<ComponentLinks component='divider' />

---

## Usage

<Example {...divider.usage} />

## API

### Divider Props

Divider inherits the props of `as` (by default, `'hr'`).

<PropsTable
  dataSource={[
    {
      prop: 'orientation',
      description: 'The orientation of the Divider.',
      type: ["'horizontal'", "'vertical'"],
      default: "'horizontal'",
    },
    {
      prop: 'className',
      description: 'Additional class names to apply to the root.',
      type: 'ClassValue',
    },
    {
      prop: 'ref',
      description: 'Ref for forwarding to the root.',
      type: 'Ref',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'hr'",
      description: 'The component or element to render as the root.',
    },
  ]}
/>
