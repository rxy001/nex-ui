---
title: Switch
description: Switch is used to toggle between on and off states.
---

import { $switch } from '@/content/_demo'

# Switch

Switch is used to toggle between on and off states.

<ComponentLinks component='switch' ariaCompatible />

---

## Anatomy

```tsx
import { Switch } from '@nex-ui/react'
```

## Usage

<Example {...$switch.usage} />

### Disabled

<Example {...$switch.disabled} />

### Sizes

<Example {...$switch.sizes} />

### Colors

<Example {...$switch.colors} />

### With Thumb Icons

<Example {...$switch.withThumbIcon} />

### With Icons

Use `startIcon` and `endIcon` to add icons to the start and end of the Switch.

<Example {...$switch.withIcons} />

### Controlled

Use `checked` and `onCheckedChange` to control the Switch state.

<Example {...$switch.controlled} />

## API

### Switch Props

Switch inherits the props of `as` (by default, `'input'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      default: '',
      description: 'The label of the Switch.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'input'",
      description: 'The component or element to render as the input.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the input inside the Switch.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'cyan'",
        "'red'",
        "'green'",
        "'pink'",
        "'purple'",
        "'gray'",
      ],
      default: "'blue'",
      description: 'The color of the Switch.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      default: "'md'",
      description: 'The size of the Switch.',
    },
    {
      prop: 'checked',
      type: 'boolean',
      description: 'If true, checks the Switch. (controlled)',
    },
    {
      prop: 'defaultChecked',
      type: 'boolean',
      description: 'If true, checks the Switch by default. (uncontrolled)',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      description: 'If true, disables the Switch.',
    },
    {
      prop: 'name',
      type: 'string',
      description: 'The name of the Switch.',
    },
    {
      prop: 'startIcon',
      type: 'ReactNode',
      description: 'The element placed before the children.',
    },
    {
      prop: 'endIcon',
      type: 'ReactNode',
      description: 'The element placed after the children.',
    },
    {
      prop: 'thumbIcon',
      type: ['ReactNode', '(state: SwitchProps) => ReactNode'],
      description: 'The icon to display when the Switch is checked.',
    },
    {
      prop: 'onChange',
      type: 'function',
      description: 'Callback fired when the checked state changes.',
    },
    {
      prop: 'onCheckedChange',
      type: '(checked: boolean) => void',
      description: 'Callback fired when the checked state changes.',
    },
    {
      prop: 'disableAnimation',
      type: 'boolean',
      default: 'false',
      description: 'If true, disables the animation for the Switch.',
    },
  ]}
/>

### Switch Slots

<SlotsTable
  dataSource={[
    {
      slot: 'root',
      class: '.nui-switch-root',
      component: "'label'",
      description: 'The root element of the Switch.',
    },
    {
      slot: 'track',
      class: '.nui-switch-track',
      component: "'span'",
      description: 'The container for the startIcon, endIcon, and thumb.',
    },
    {
      slot: 'startIcon',
      class: '.nui-switch-start-icon',
      component: "'span'",
      description: 'The container for the start icon inside the Switch.',
    },
    {
      slot: 'endIcon',
      class: '.nui-switch-end-icon',
      component: "'span'",
      description: 'The container for the end icon inside the Switch.',
    },
    {
      slot: 'thumb',
      class: '.nui-switch-thumb',
      component: "'span'",
      description: 'The slider inside the Switch.',
    },
    {
      slot: 'label',
      class: '.nui-switch-label',
      component: "'span'",
      description: 'The label of the Switch.',
    },
  ]}
/>
