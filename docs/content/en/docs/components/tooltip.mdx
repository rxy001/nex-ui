---
title: Tooltip
description: Tooltip displays brief contextual information when users interact with an element.
---

import { tooltip } from '@/content/_demo'

# Tooltip

Tooltips display brief contextual information when users interact with an element.

<ComponentLinks component='tooltip' ariaCompatible />

---

## Anatomy

```tsx
import { Tooltip } from '@nex-ui/react'
```

## Usage

<Example {...tooltip.usage} />

### Colors

<Example {...tooltip.colors} />

### Placements

<Example {...tooltip.placements} />

### Sizes

<Example {...tooltip.sizes} />

### Offset

Use the `offset` prop to set main-axis and cross-axis offsets. Set it to `false` to disable offset.

<Example {...tooltip.offset} />

### Flip

Use the `flip` prop to automatically reposition the Tooltip when it overflows. The default is `{ mainAxis: true, crossAxis: true }`. Set it to `false` to disable flipping.

<Example {...tooltip.flip} />

### Shift

Use the `shift` prop to keep the Tooltip in view. Defaults to `true`.

<Example {...tooltip.shift} />

### Interactive

Use the `interactive` prop to keep Tooltip content visible during interaction. Defaults to `false`.

<Example {...tooltip.interactive} />

### Delay

Use `openDelay` and `closeDelay` to set the show/hide delay (in milliseconds).

<Example {...tooltip.delay} />

### Controlled

<Example {...tooltip.controlled} />

## API

### Tooltip Props

Tooltip inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'content',
      type: 'ReactNode',
      description: 'The content shown by Tooltip.',
    },
    {
      prop: 'children',
      type: 'ReactElement',
      description: 'The trigger element of Tooltip.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the Tooltip root element.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description:
        'The component used for the Tooltip root element. It can be an HTML element string or a custom component.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'container',
      type: ['HTMLElement | null', '() => HTMLElement | null'],
      description: 'The container element for the portal.',
      default: 'document.body',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: 'If true, opens the Tooltip. (controlled)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'If true, opens the Tooltip by default. (uncontrolled)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description: 'Callback fired when Tooltip opens or closes.',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description: 'Callback fired when Tooltip closes.',
    },
    {
      prop: 'placement',
      type: [
        `'top'`,
        `'top-start'`,
        `'top-end'`,
        `'bottom'`,
        `'bottom-start'`,
        `'bottom-end'`,
        `'left'`,
        `'left-start'`,
        `'left-end'`,
        `'right'`,
        `'right-start'`,
        `'right-end'`,
      ],
      default: `'top'`,
      description: 'The placement of the Tooltip.',
    },
    {
      prop: 'size',
      type: `'sm' | 'md' | 'lg'`,
      default: `'md'`,
      description: 'The size of the Tooltip.',
    },
    {
      prop: 'color',
      type: [
        `'default'`,
        `'blue'`,
        `'orange'`,
        `'yellow'`,
        `'cyan'`,
        `'red'`,
        `'green'`,
        `'pink'`,
        `'purple'`,
        `'gray'`,
      ],
      default: `'default'`,
      description: 'The color of the Tooltip.',
    },
    {
      prop: 'radius',
      type: [`'none'`, `'sm'`, `'md'`, `'lg'`, `'full'`],
      default: `'md'`,
      description: 'The border radius of the Tooltip.',
    },
    {
      prop: 'maxWidth',
      type: ['string', 'number'],
      description: 'The maximum width of the Tooltip.',
    },
    {
      prop: 'motionProps',
      type: 'HTMLMotionProps',
      description: 'The props to modify the framer motion animation.',
    },
    {
      prop: 'offset',
      type: ['false', 'number', '{ mainAxis?: number; crossAxis?: number }'],
      default: '5',
      description: 'Sets Tooltip offset, or disables offset.',
    },
    {
      prop: 'flip',
      type: ['false', '{ mainAxis?: boolean; crossAxis?: boolean }'],
      default: '{ mainAxis: true, crossAxis: true }',
      description: 'Changes Tooltip placement to keep it in view.',
    },
    {
      prop: 'shift',
      type: 'boolean',
      default: 'true',
      description: 'If true, shifts the Tooltip to keep it in view.',
    },
    {
      prop: 'interactive',
      type: 'boolean',
      default: 'false',
      description:
        'If true, keeps the Tooltip open when interacting with its content.',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      default: 'false',
      description:
        'If true, keeps the Tooltip mounted in the DOM when not open.',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the Tooltip when the Escape key is pressed.',
    },
    {
      prop: 'closeOnClick',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the Tooltip when the trigger is clicked while the Tooltip is open.',
    },
    {
      prop: 'closeOnDetached',
      type: 'boolean',
      default: 'true',
      description:
        'If true, closes the Tooltip when the trigger element is fully obscured.',
    },
    {
      prop: 'openDelay',
      type: 'number',
      default: '100',
      description: 'Delay in milliseconds before showing the Tooltip.',
    },
    {
      prop: 'closeDelay',
      type: 'number',
      default: '100',
      description: 'Delay in milliseconds before hiding the Tooltip.',
    },
    {
      prop: 'disableAnimation',
      type: 'boolean',
      default: 'false',
      description: 'If true, disables the animation for the Tooltip.',
    },
  ]}
/>

### Tooltip Slots

<SlotsTable
  dataSource={[
    {
      slot: 'paper',
      description: 'The content container of Tooltip.',
      class: '.nui-tooltip-paper',
      component: "'div'",
    },
  ]}
/>
