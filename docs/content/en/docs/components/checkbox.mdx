---
title: Checkbox
description: The checkbox allows users to select one or more items from a collection.
---

import { checkbox } from '@/content/_demo'
import { type } from 'os'

# Checkbox

The checkbox allows users to select one or more items from a collection.

<ComponentLinks component='checkbox' ariaCompatible />

## Usage

<Example {...checkbox.usage} />

### Disabled

<Example {...checkbox.disabled} />

### Sizes

<Example {...checkbox.sizes} />

### Colors

<Example {...checkbox.colors} />

### Icon

<Example {...checkbox.icon} />

### Controlled

<Example {...checkbox.controlled} />

## Checkbox Group

<Example {...checkbox.groupUseage} />

### Vertical

<Example {...checkbox.groupVertical} />

### Disabled

<Example {...checkbox.groupDisabled} />

## API

### Checkbox Props

The checkbox props inherit the props of `as` (by default, it is the `input` tag).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The label of the checkbox.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'ryan'",
        "'red'",
        "'green'",
        "'pick'",
        "'purple'",
        "'gray'",
      ],
      default: "'blue'",
      description: 'The color of the checkbox.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      default: "'md'",
      description: 'The size of the checkbox.',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'full'"],
      default: 'size',
      description: 'The border radius of the checkbox.',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'If true, the checkbox is disabled.',
    },
    {
      prop: 'checked',
      type: 'boolean',
      description: 'If true, the checkbox is checked.(controlled)',
    },
    {
      prop: 'defaultChecked',
      type: 'boolean',
      description: 'The default checked state.(uncontrolled)',
    },
    {
      prop: 'icon',
      type: 'ReactNode | (ownerState) => ReactNode',
      description: 'The icon to be displayed when the checkbox is checked.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root element.',
    },
    {
      prop: 'name',
      type: 'string',
      description:
        'The name of the CheckboxGroup, used when submitting an HTML form.',
    },
    {
      prop: 'value',
      type: 'string',
      description: 'The value of the checkbox.(controlled)',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root element.',
    },
    {
      prop: 'onChange',
      type: '(event: ChangeEvent<HTMLInputElement>) => void',
      description:
        "Handler that is called when the element's checked state changes.",
    },
    {
      prop: 'onCheckedChange',
      type: '(checked: boolean) => void',
      description:
        "Handler that is called when the element's checked state changes.",
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'input'",
      description: 'The component used for the input element.',
    },
    {
      prop: 'sx',
      type: 'SxProp<CheckboxOwnerState>',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classes',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
  ]}
/>

### Checkbox Slots

<SlotsTable
  dataSource={[
    {
      slot: 'root',
      class: '.nui-checkbox-root',
      component: "'label'",
      description: 'The root element of the checkbox.',
    },
    {
      slot: 'icon',
      class: '.nui-checkbox-icon',
      component: "'span'",
      description: 'The container of the icon inside the checkbox.',
    },
    {
      slot: 'label',
      class: '.nui-checkbox-label',
      component: "'span'",
      description: 'The label of the checkbox.',
    },
  ]}
/>

### Checkbox Group Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The checkboxes items.',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      description: 'If true, the checkboxes are disabled.',
    },
    {
      prop: 'name',
      type: ['string', 'number'],
      description:
        'The name of the CheckboxGroup, used when submitting an HTML form.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'ryan'",
        "'red'",
        "'green'",
        "'pick'",
        "'purple'",
        "'gray'",
      ],
      default: "'blue'",
      description: 'The color of the checkboxes.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      default: "'md'",
      description: 'The size of the checkboxes.',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'full'", "'none'"],
      default: 'size',
      description: 'The border radius of the checkboxes.',
    },
    {
      prop: 'value',
      type: ['T extends (string | number)[]'],
      description: 'The current selected values. (controlled)',
    },
    {
      prop: 'defaultValue',
      type: ['T extends (string | number)[]'],
      description: 'The default checked values. (uncontrolled)',
    },
    {
      prop: 'onValueChange',
      type: '(value: T[]) => void',
      description: 'Handler that is called when the value changes.',
    },
  ]}
/>
