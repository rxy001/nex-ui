---
title: Input
description: Input allows users to provide text input.
---

import { input } from '@/content/_demo'

# Input

Input allows users to provide text input.

<ComponentLinks component='input' />

---

## Anatomy

```tsx
import { Input } from '@nex-ui/react'
```

## Usage

<Example {...input.usage} />

### Disabled

<Example {...input.disabled} />

### ReadOnly

<Example {...input.readOnly} />

### Variants

<Example {...input.variants} />

### Sizes

<Example {...input.sizes} />

### Colors

<Example {...input.colors} />

### Label Placements

Use the `labelPlacement` prop (`inside`, `outside`, `float-outside`, or `float-inside`) to change the label position.

<Example {...input.labelPlacements} />

### Clear Button

Set `clearable` to `true` to show a clear button at the end of the Input. The button appears only when the input has a value.

<Example {...input.clearable} />

### Prefix & Suffix

Use `prefix` and `suffix` to add elements before or after the input.

<Example {...input.icons} />

### Invalid

<Example {...input.invalid} />

## API

### Input Props

Input inherits the props of `as` (by default, `'input'`).

<PropsTable
  dataSource={[
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the input inside the Input.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'input'",
      description: 'The component or element to render as the input.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'cyan'",
        "'red'",
        "'green'",
        "'pink'",
        "'purple'",
        "'gray'",
      ],
      default: "'blue'",
      description: 'The color of the Input.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      default: "'md'",
      description: 'The size of the Input.',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'full'", "'none'"],
      default: "'md'",
      description: 'The border radius of the Input.',
    },
    {
      prop: 'variant',
      type: ["'faded'", "'outlined'", "'underlined'"],
      default: "'outlined'",
      description: 'The Input appearance style.',
    },
    {
      prop: 'placeholder',
      type: 'string',
      description: 'The placeholder of the Input.',
    },
    {
      prop: 'type',
      type: [
        "'text'",
        "'email'",
        "'password'",
        "'number'",
        "'search'",
        "'tel'",
        "'url'",
        "'date'",
        "'datetime-local'",
        "'month'",
        "'week'",
        "'time'",
      ],
      default: "'text'",
      description: 'The type of the input element.',
    },
    {
      prop: 'value',
      type: 'string',
      description: 'The value of the Input. (controlled)',
    },
    {
      prop: 'defaultValue',
      type: 'string',
      description: 'The default value of the Input. (uncontrolled)',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'If true, disables the Input.',
    },
    {
      prop: 'name',
      type: 'string',
      description: 'The name of the Input.',
    },
    {
      prop: 'prefix',
      type: 'ReactNode',
      description: 'The element placed before the input.',
    },
    {
      prop: 'suffix',
      type: 'ReactNode',
      description: 'The element placed after the input.',
    },
    {
      prop: 'fullWidth',
      type: 'boolean',
      default: 'false',
      description:
        'If true, makes the Input take the full width of its parent.',
    },
    {
      prop: 'label',
      type: 'ReactNode',
      description: 'The label of the Input.',
    },
    {
      prop: 'labelPlacement',
      type: ["'inside'", "'outside'", "'float-outside'", "'float-inside'"],
      default: "'float-outside'",
      description: 'The position of the label.',
    },
    {
      prop: 'invalid',
      type: 'boolean',
      default: 'false',
      description: 'If true, marks the Input as invalid.',
    },
    {
      prop: 'clearable',
      type: 'boolean',
      default: 'false',
      description: 'If true, shows a clear button for the Input.',
    },
    {
      prop: 'onChange',
      type: 'function',
      description: 'Callback fired when the value changes.',
    },
    {
      prop: 'onValueChange',
      type: '(value: string) => void',
      description: 'Callback fired when the value changes.',
    },
    {
      prop: 'onClear',
      type: 'function',
      description: 'Callback fired when the clear button is clicked.',
    },
    {
      prop: 'disableAnimation',
      type: 'boolean',
      default: 'false',
      description: 'If true, disables the animation for the Input.',
    },
  ]}
/>

### Input Slots

<SlotsTable
  dataSource={[
    {
      slot: 'root',
      class: '.nui-input-root',
      component: "'div'",
      description: 'The root element of the Input.',
    },
    {
      slot: 'label',
      class: '.nui-input-label',
      component: "'label'",
      description: 'The label element of the Input.',
    },
    {
      slot: 'clearButton',
      class: '.nui-input-clear-btn',
      component: "'button'",
      description: 'The clear button of the Input.',
    },
    {
      slot: 'prefix',
      class: '.nui-input-prefix',
      component: "'span'",
      description: 'The prefix element of the Input.',
    },
    {
      slot: 'suffix',
      class: '.nui-input-suffix',
      component: "'span'",
      description: 'The suffix element of the Input.',
    },
  ]}
/>
