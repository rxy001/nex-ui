---
title: Avatar
description: Avatar represents users with profile photos, initials, or fallback icons.
---

import { avatar } from '@/content/_demo'

# Avatar

Avatar represents users with profile images, initials, or fallback icons.

<ComponentLinks component='avatar' />

---

## Anatomy

NexUI provides two components for building avatars:

- `Avatar`: The component that displays a single user.
- `AvatarGroup`: The component that displays a group of users.

```tsx
import { Avatar, AvatarGroup } from '@nex-ui/react'
```

## Usage

<Example {...avatar.usage} />

### Sizes

<Example {...avatar.sizes} />

### Radii

<Example {...avatar.radii} />

### Outlined

<Example {...avatar.outlined} />

### Colors

<Example {...avatar.colors} />

### Fallbacks

Avatar provides fallback mechanisms in this order when image loading fails:

- `children` prop content
- First letter of `alt` text

<Example {...avatar.fallback} />

## AvatarGroup

### Group Max Count

Use the `max` prop to limit how many Avatars are shown in an AvatarGroup.

<Example {...avatar.groupMaxCount} />

### Group Total Count

Use the `total` prop to set the total number of Avatars in the AvatarGroup.

<Example {...avatar.groupTotalCount} />

### Custom Spacing

Use the `spacing` prop to set the spacing between Avatars in the AvatarGroup.

<Example {...avatar.groupSpacing} />

### Custom Surplus

`renderSurplus` allows custom rendering of overflow Avatars in `AvatarGroup`. Set it to a function that receives the surplus count based on `children` and `max`.

<Example {...avatar.groupCustomSurplus} />

## API

### Avatar Props

Avatar inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      default: '',
      description:
        'Renders icon or text content inside the Avatar when `src` is not provided. This can be a React element or a plain string.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'", "'xl'"],
      default: "'md'",
      description: 'The size of the Avatar.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'cyan'",
        "'red'",
        "'green'",
        "'pink'",
        "'purple'",
        "'gray'",
      ],
      default: "'gray'",
      description: 'The color of the Avatar.',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'xl'", "'full'", "'none'"],
      default: 'size',
      description: 'The border radius of the Avatar.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'src',
      type: 'string',
      description: 'The src attribute for the img element.',
    },
    {
      prop: 'srcSet',
      type: 'string',
      description: 'The srcSet attribute for the img element.',
    },
    {
      prop: 'alt',
      type: 'string',
      description:
        'Used in combination with src or srcSet to provide an alt attribute for the rendered img element.',
    },
    {
      prop: 'outlined',
      type: 'boolean',
      default: 'false',
      description: 'If true, adds an outline around the Avatar.',
    },
  ]}
/>

### Avatar Slots

<SlotsTable
  dataSource={[
    {
      slot: 'img',
      class: '.nui-avatar-img',
      component: "'img'",
      description: 'The image element slot.',
    },
  ]}
/>

### AvatarGroup Props

AvatarGroup inherits the props of `as` (by default, `'div'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      default: '',
      description: 'Usually contains Avatar components.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'", "'xl'"],
      default: "'md'",
      description: 'The size of all Avatars in the group.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'cyan'",
        "'red'",
        "'green'",
        "'pink'",
        "'purple'",
        "'gray'",
      ],
      default: "'gray'",
      description: 'The color of all Avatars in the group.',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'xl'", "'full'", "'none'"],
      default: 'size',
      description: 'The border radius of all Avatars in the group.',
    },
    {
      prop: 'outlined',
      type: 'boolean',
      default: 'false',
      description: 'If true, adds an outline around all Avatars in the group.',
    },
    {
      prop: 'max',
      type: 'number',
      default: '5',
      description:
        'Maximum number of Avatars to show before displaying the +x surplus indicator.',
    },
    {
      prop: 'total',
      type: 'number',
      description: 'The total number of Avatars.',
    },
    {
      prop: 'spacing',
      type: 'string | number',
      description: 'Spacing between Avatars.',
    },
    {
      prop: 'renderSurplus',
      type: '(surplus: number) => ReactNode',
      default: '',
      description:
        'Custom renderer for surplus Avatars. Receives the surplus count based on children and max.',
    },
    {
      prop: 'disableAnimation',
      type: 'boolean',
      default: 'false',
      description: 'If true, disables the animation when hovering Avatar.',
    },
  ]}
/>

### AvatarGroup Slots

<SlotsTable
  dataSource={[
    {
      slot: 'surplus',
      class: '.nui-avatar-group-surplus',
      component: 'Avatar',
      description: 'The component used to display surplus Avatars.',
    },
  ]}
/>
