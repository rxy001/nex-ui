---
title: Button
description: Button allows users to perform actions and make choices with a single click.
---

import { button } from '@/content/_demo'

# Button

Button allows users to perform actions and make choices with a single click.

<ComponentLinks component='button' ariaCompatible />

---

## Usage

<Example {...button.usage} />

### Disabled

<Example {...button.disabled} />

### Sizes

<Example {...button.sizes} />

### Radii

<Example {...button.radii} />

### Colors

<Example {...button.colors} />

### Variants

<Example {...button.variants} />

### Loading

Enable the `loading` prop to display a spinner indicator within the Button.

<Example {...button.loading} />

Customize the loading indicator using the `spinner` prop.

### With Icons

Add icons to the Button using the `startIcon` or `endIcon` props.

<Example {...button.withIcons} />

### Icon Only

Enable the `iconOnly` prop for square-shaped, icon-only Buttons.

<Example {...button.iconOnly} />

## API

### Button Props

Button inherits the props of `as` (by default, `'button'`).

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'The content of the Button.',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'cyan'",
        "'red'",
        "'green'",
        "'pink'",
        "'purple'",
        "'gray'",
      ],
      default: "'blue'",
      description: 'The color of the Button.',
    },
    {
      prop: 'variant',
      type: ["'solid'", "'outlined'", "'ghost'", "'faded'"],
      default: "'solid'",
      description: 'The Button appearance style.',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      default: "'md'",
      description: 'The size of the Button.',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'full'", "'none'"],
      default: 'size',
      description: 'The border radius of the Button.',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'If true, the Button is disabled.',
    },
    {
      prop: 'loading',
      type: 'boolean',
      default: 'false',
      description:
        'If true, the loading indicator is visible and the Button is disabled.',
    },
    {
      prop: 'fullWidth',
      type: 'boolean',
      default: 'false',
      description: 'If true, the Button takes the full width of its parent.',
    },
    {
      prop: 'iconOnly',
      type: 'boolean',
      default: 'false',
      description: 'If true, makes the Button square (equal width and height).',
    },
    {
      prop: 'startIcon',
      type: 'ReactNode',
      description: 'The element placed before the children.',
    },
    {
      prop: 'endIcon',
      type: 'ReactNode',
      description: 'The element placed after the children.',
    },
    {
      prop: 'spinner',
      type: 'ReactNode',
      description: 'Spinner to display when loading.',
    },
    {
      prop: 'spinnerPlacement',
      type: ["'start'", "'end'"],
      default: "'start'",
      description: 'The spinner placement.',
    },
    {
      prop: 'rippleDisabled',
      type: 'boolean',
      default: 'false',
      description: 'If true, the ripple effect is disabled.',
    },
    {
      prop: 'href',
      type: 'string',
      description:
        'The URL to link to when the Button is clicked. If defined, an anchor element will be used as the root node.',
    },
    {
      prop: 'target',
      type: 'string',
      description: 'Specifies where to display the linked URL.',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: 'Additional class names to apply to the root.',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: 'Ref for forwarding to the root.',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'button'",
      description: 'The component or element to render as the root.',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description:
        'The system prop that allows defining system overrides as well as additional CSS styles.',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'The className used for each slot.',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'The props used for each slot.',
    },
  ]}
/>

### Button Slots

<SlotsTable
  dataSource={[
    {
      slot: 'startIcon',
      class: '.nui-button-start-icon',
      component: "'span'",
      description:
        'The container for the icon before the child element of the Button.',
    },
    {
      slot: 'endIcon',
      class: '.nui-button-end-icon',
      component: "'span'",
      description:
        'The container for the icon after the child element of the Button.',
    },
  ]}
/>
