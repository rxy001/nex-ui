---
title: Slot Recipes
description: Slot Recipes let you style multiple parts of a single component.
---

# Slot Recipes

Slot Recipes let you split a component into multiple stylable parts. Before reading this section, we recommend reviewing [Recipes](/docs/styling/recipes).

## Basic Usage

You can use the `defineSlotRecipe` API to define a slot recipe.

```tsx {4-8}
import { defineSlotRecipe } from '@nex-ui/react'

export const checkboxSlotRecipe = defineSlotRecipe({
  slots: {
    root: { display: 'flex', alignItems: 'center', gap: '2' },
    control: { borderWidth: '1px', borderRadius: 'sm' },
    label: { padding: '2' },
  },
})

const { root, control, label } = checkboxSlotRecipe()

/**
 * Result:
 * root: {
 *   display: 'flex',
 *   alignItems: 'center',
 *   gap: '2'
 * }
 * control: {
 *   borderWidth: '1px',
 *   borderRadius: 'sm'
 * }
 * label: {
 *   padding: '2'
 * }
 */
```

## Slots With Variants

You can use `variants` to update the entire component and all its slots together.

```tsx {9-20}
import { defineSlotRecipe } from '@nex-ui/react'

export const checkboxSlotRecipe = defineSlotRecipe({
  slots: {
    root: { display: 'flex', alignItems: 'center', gap: '2' },
    control: { borderWidth: '1px', borderRadius: 'sm' },
    label: { padding: '2' },
  },
  variants: {
    size: {
      sm: {
        control: { width: '8', height: '8' },
        label: { fontSize: 'sm' },
      },
      md: {
        control: { width: '10', height: '10' },
        label: { fontSize: 'md' },
      },
    },
  },
})

const { root, control, label } = checkboxSlotRecipe({
  size: 'sm',
})

/**
 * Result:
 * root: {
 *   display: 'flex',
 *   alignItems: 'center',
 *   gap: '2'
 * }
 * control: {
 *   borderWidth: '1px',
 *   borderRadius: 'sm'
 *   width: '8'
 *   height: '8'
 * }
 * label: {
 *   padding: '2',
 *   fontSize: 'sm'
 * }
 */

const { root, control, label } = checkboxSlotRecipe({
  size: 'md',
})

/**
 * Result:
 * root: {
 *   display: 'flex',
 *   alignItems: 'center',
 *   gap: '2'
 * }
 * control: {
 *   borderWidth: '1px',
 *   borderRadius: 'sm'
 *   width: '10'
 *   height: '10'
 * }
 * label: {
 *   padding: '2',
 *   fontSize: 'md'
 * }
 */
```

## Slots With Compound Variants

Similar to `variants`, when using `compoundVariants`, you can also define styles for each slot.

```tsx {29-38}
import { defineSlotRecipe } from '@nex-ui/react'

export const checkboxSlotRecipe = defineSlotRecipe({
  slots: {
    root: { display: 'flex', alignItems: 'center', gap: '2' },
    control: { borderWidth: '1px', borderRadius: 'sm' },
    label: { padding: '2' },
  },
  variants: {
    size: {
      sm: {
        control: { width: '8', height: '8' },
        label: { fontSize: 'sm' },
      },
      md: {
        control: { width: '10', height: '10' },
        label: { fontSize: 'md' },
      },
    },
    color: {
      primary: {
        label: { color: 'blue.500' },
      },
      secondary: {
        label: { color: 'blue.500' },
      },
    },
  },
  compoundVariants: [
    {
      size: ['sm', 'md'],
      color: 'primary',
      css: {
        label: { opacity: 0.7 },
        control: { opacity: 0.5 },
      },
    },
  ],
})
```

## Slots With Extends

`defineSlotRecipe` can extend another SlotRecipe, including its `slots`, `variants`, `defaultVariants`, and `compoundVariants`.
It automatically merges matching keys and provides TypeScript hints.

```tsx {41-45}
import { defineSlotRecipe } from '@nex-ui/react'

export const checkboxSlotRecipe = defineSlotRecipe({
  slots: {
    root: { display: 'flex', alignItems: 'center', gap: '2' },
    control: { borderWidth: '1px', borderRadius: 'sm' },
    label: { padding: '2' },
  },
  variants: {
    size: {
      sm: {
        control: { width: '8', height: '8' },
        label: { fontSize: 'sm' },
      },
      md: {
        control: { width: '10', height: '10' },
        label: { fontSize: 'md' },
      },
    },
    color: {
      primary: {
        label: { color: 'blue.500' },
      },
      secondary: {
        label: { color: 'blue.500' },
      },
    },
  },
  compoundVariants: [
    {
      size: ['sm', 'md'],
      color: 'primary',
      css: {
        label: { opacity: 0.7 },
        control: { opacity: 0.5 },
      },
    },
  ],
})

const extendedSlotRecipe = defineSlotRecipe({
  extend: checkboxSlotRecipe,
  slots: {},
  variants: {},
  compoundVariants: [],
})
```
