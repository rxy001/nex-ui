---
title: Selectors
description: Selectors let you compose multiple CSS selectors for more flexible and maintainable styling.
---

import { Box } from '@nex-ui/react'
import { Callout } from 'nextra/components'
import { SelectorTable } from '@/components/docs'

# Selectors

Selectors let you compose multiple CSS selectors to apply style rules more efficiently and flexibly.

## Customizing Selectors

Selectors can be defined in the `theme.selectors` section of your theme configuration:

```tsx
import { defineTheme } from '@nex-ui/react'

const theme = defineTheme({
  selectors: {
    hover: '&:not(:disabled):not([data-disabled=true]):hover',
    active: '&:not(:disabled):not([data-disabled=true]):active',
  },
})
```

The type of the Selector value is `string`.

### TypeScript

If your project uses TypeScript, you can extend custom selectors through [Module Augmentation](https://www.typescriptlang.org/docs/handbook/declaration-merging.html#module-augmentation).

```tsx
declare module '@nex-ui/react' {
  interface SelectorsOverrides {
    hover: '&:not(:disabled):not([data-disabled=true]):hover'
    active: '&:not(:disabled):not([data-disabled=true]):active'
  }
}
```

## Using Selectors

After defining selectors, you can use them in the [`sx`](/docs/styling/sx-prop) prop, [`defineRecipe`](/docs/styling/recipes), or [`defineSlotRecipe`](/docs/styling/slot-recipes).

> The Breakpoints object can be used as Selectors by default.

<FlexCenter gap='10'>
  <Box
    as='button'
    sx={{
      color: 'purple.500',
      _hover: {
        color: 'blue.500',
      },
      _active: {
        color: 'green.500',
      },
    }}
  >
    Clickable
  </Box>
  <Box
    as='button'
    disabled
    sx={{
      color: 'purple.500',
      _hover: {
        color: 'blue.500',
      },
      _active: {
        color: 'green.500',
      },
    }}
  >
    Disabled
  </Box>
</FlexCenter>

```tsx
import { Box, Flex } from '@nex-ui/react'

export default function App() {
  return (
    <Flex gap='10'>
      <Box
        as='button'
        sx={{
          color: 'purple.500',
          _hover: {
            color: 'blue.500',
          },
          _active: {
            color: 'green.500',
          },
        }}
      >
        Clickable
      </Box>
      <Box
        as='button'
        disabled
        sx={{
          color: 'purple.500',
          _hover: {
            color: 'blue.500',
          },
          _active: {
            color: 'green.500',
          },
        }}
      >
        Disabled
      </Box>
    </Flex>
  )
}
```

In the example above, `color` is defined separately for three interaction states (default, hover, and active), which mirrors traditional CSS structure. You can also simplify this in another form:

```tsx
import { Box, Flex } from '@nex-ui/react'

export default function App() {
  return (
    <Box
      as='button'
      sx={{
        color: {
          _DEFAULT: 'purple.500',
          _hover: 'blue.500',
          _active: 'green.500',
        },
      }}
    >
      Clickable
    </Box>
  )
}
```

In this version, `color` styles for different states are grouped into a single object as a **state mapping**. This makes state transitions easier to read, and is especially convenient when managing a single property.

<Callout>
  In complex style scenarios, it is more recommended to use the first form that
  conforms to CSS standards, which is more scalable. As the complexity of the
  style increases, more styles can be easily added through the same state,
  making it easy to understand and maintain.
</Callout>

## Available Default Selectors

<SelectorTable />
