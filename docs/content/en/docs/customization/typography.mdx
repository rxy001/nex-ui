---
title: Typography
description: Typography tokens are used to manage and standardize typography styles, ensuring a unified visual style and hierarchy of text throughout the application.
---

import { Callout } from 'nextra/components'
import { AvailableProps, TokensTable } from '@/components/docs'
import { map } from '@nex-ui/utils'
import { defaultConfig } from '@nex-ui/react'

# Typography

Typography tokens are used to manage and standardize typography styles, including [FontSizes](#fontsizes), [FontFamilies](#fontfamilies), [FontWeights](#fontweights), [LineHeights](#lineheights), etc. They ensure a unified visual style and hierarchy of text throughout the application.

## FontSizes

FontSize tokens are a set of preset font sizes.

<Callout type='info'>
  <span className='x:float-left'>
    By default, fontSize tokens can be directly applied to the following
    properties&nbsp;
  </span>
  <AvailableProps.FontSizes />
  <span>.</span>
</Callout>

<TokensTable
  dataSource={map(defaultConfig.tokens.fontSizes, (value, key) => ({
    token: `fontSizes.${key}`,
    value,
  }))}
/>

## FontFamilies

FontFamily tokens are a set of preset fonts.

<Callout type='info'>
  <span className='x:float-left'>
    By default, fontFamily tokens can be directly applied to the following
    properties&nbsp;
  </span>
  <AvailableProps.FontFamilies />
  <span>.</span>
</Callout>

<TokensTable
  dataSource={map(defaultConfig.tokens.fontFamilies, (value, key) => ({
    token: `fontFamilies.${key}`,
    value,
  }))}
/>

## FontWeights

FontWeight tokens are a set of preset font weights.

<Callout type='info'>
  <span className='x:float-left'>
    By default, fontWeight tokens can be directly applied to the following
    properties&nbsp;
  </span>
  <AvailableProps.FontWeights />
  <span>.</span>
</Callout>

<TokensTable
  dataSource={map(defaultConfig.tokens.fontWeights, (value, key) => ({
    token: `fontWeights.${key}`,
    value,
  }))}
/>

## LineHeights

LineHeight tokens are a set of preset line heights.

<Callout type='info'>
  <span className='x:float-left'>
    By default, lineHeight tokens can be directly applied to the following
    properties&nbsp;
  </span>
  <AvailableProps.LineHeights />
  <span>.</span>
</Callout>

<TokensTable
  dataSource={map(defaultConfig.tokens.lineHeights, (value, key) => ({
    token: `lineHeights.${key}`,
    value,
  }))}
/>
