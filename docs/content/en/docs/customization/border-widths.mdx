---
title: BoderWidths
description: BorderWidth tokens are a set of preset border widths.
---

import { Callout } from 'nextra/components'
import { AvailableProps, TokensTable } from '../../../_components'
import { map } from '@nex-ui/utils'
import { defaultConfig } from '@nex-ui/react'

# BorderWidths

BorderWidth tokens are a set of preset border widths.

<Callout type='info'>
  <span className='x:float-left'>
    By default, borderWidth tokens can be directly applied to the following
    properties&nbsp;
  </span>
  <AvailableProps.BorderWidths />
  <span>.</span>
</Callout>

Nex UI supports the following tokens out of the box.

<TokensTable
  dataSource={map(defaultConfig.tokens.borderWidths, (value, key) => ({
    token: `borderWidths.${key}`,
    value,
  }))}
/>
