---
title: Sizes
description: Sizes Tokens 是一组标准化的尺寸值，统一管理 UI 元素的宽度、高度、最大/最小尺寸等属性。
---

import { Callout } from 'nextra/components'
import { AvailableProps, TokensTable } from './_components'
import { defaultConfig } from '@nex-ui/react'

# Sizes

Sizes Tokens 是一组标准化的尺寸值，统一管理 UI 元素的宽度、高度、最大/最小尺寸等属性。

<Callout type='info'>
  <span className='x:float-left'>
    默认情况下，Sizes 类型的 Tokens 可直接应用于以下属性&nbsp;
  </span>
  <AvailableProps.Sizes />
  <span>。</span>
</Callout>

Nex UI 开箱即支持以下 Tokens。

export const dataSource = (() => {
  return Object.entries(defaultConfig.tokens.sizes)
    .map(([key, value]) => {
      const numValue = parseFloat(value);
      return { token:key, value: value, numValue };
    })
    .sort((a, b) => a.numValue - b.numValue) // 按像素值排序
    .map(({ numValue, ...rest }) => rest); // 移除临时属性

})()

<TokensTable dataSource={dataSource} />
