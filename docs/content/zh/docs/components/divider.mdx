---
title: Divider
description: Divider 用于分隔页面中的内容。
---

import { divider } from '@/content/_demo'

# Divider

Divider 用于分隔页面中的内容。

<ComponentLinks component='divider' />

## 使用

<Example {...divider.usage} />

## API

### Divider Props

Divider Props 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'orientation',
      description: 'Divider 的方向。',
      type: ["'horizontal'", "'vertical'"],
      default: "'horizontal'",
    },
    {
      prop: 'className',
      description: '用于根元素的 className。',
      type: 'ClassValue',
    },
    {
      prop: 'ref',
      description: '用于转发根元素。',
      type: 'Ref',
    },
    {
      prop: 'sx',
      type: 'SxProp<DividerOwnerState>',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
    },
  ]}
/>
