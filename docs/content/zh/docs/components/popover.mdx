---
title: Popover
description: Popover 是一种非模态对话框，会悬浮在其触发元素周围。它通常用于在某个元素之上叠加展示额外的丰富内容。
---

import { popover } from '@/content/_demo'

# Popover

Popover 是一种非模态对话框，会悬浮在其触发元素周围。它通常用于在某个元素之上叠加展示额外的丰富内容。

<ComponentLinks component='popover' />

---

## Anatomy

NexUI 导出了 4 个 Popover 相关组件:

- `Popover`: 用于显示 Popover 的根组件。
- `PopoverTrigger`: 用于触发 Popover 打开的组件。
- `PopoverContent`: 用于显示 Popover 内容的组件。
- `PopoverClose`: 用于触发 Popover 关闭的组件。

```tsx
import {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverClose,
} from '@nex-ui/react'
```

## Usage

<Example {...popover.usage} />

### Colors

<Example {...popover.colors} />

### Placements

<Example {...popover.placements} />

### Offset

PopoverContent 的 `offset` 属性可以设置 Popover 偏移量，包括主轴和交叉轴的偏移。设置 false 可禁用偏移。

<Example {...popover.offset} />

### Flip

PopoverContent 的 `flip` 属性可以更改 Popover 的位置，使其保持在视图中。默认值为 `{ mainAxis: true, crossAxis: true }`。设置 `false` 可禁用翻转行为。

<Example {...popover.flip} />

### Shift

PopoverContent 的 `shift` 属性可以移动 Popover 以保持其在视图中。默认为 true。

<Example {...popover.shift} />

### With Form

Popover 会处理其弹窗内容内的焦点。这意味着您可以将 Popover 与表单元素搭配使用，完全没有问题。当 Popover 关闭时，焦点会返回至触发元素。

<Example {...popover.withForm} />

### Controlled

<Example {...popover.controlled} />

## API

### Popover Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '通常是 PopoverTrigger 和 PopoverContent 组件。',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: '控制 Popover 的打开状态。(受控)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'Popover 的默认打开状态。(非受控)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description: '当 Popover 打开/关闭时触发回调函数。',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description: '当 Popover 关闭时触发回调函数。',
    },
  ]}
/>

### PopoverContent Props

PopoverContent 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Popover 的内容。',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: '用于转发 Popover 根元素的 ref。',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: '用于 Popover 根元素的 className。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'div'",
      description:
        '用于 Popover 根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: '用于 Popover 根元素的 sx 属性。',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: '用于 Popover 各个部分的 classNames。',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: '用于 Popover 各个部分的 slotProps。',
    },
    {
      prop: 'color',
      type: [
        `'default'`,
        `'blue'`,
        `'orange'`,
        `'yellow'`,
        `'cyan'`,
        `'red'`,
        `'green'`,
        `'pink'`,
        `'purple'`,
        `'gray'`,
      ],
      default: `'default'`,
      description: 'Popover 的颜色。',
    },
    {
      prop: 'maxWidth',
      type: ['string', 'number'],
      description: 'Popover 的最大宽度。',
    },
    {
      prop: 'width',
      type: ['string', 'number'],
      default: 'auto',
      description: 'Popover 的宽度。',
    },
    {
      prop: 'radius',
      type: [`'none'`, `'sm'`, `'md'`, `'lg'`, `'full'`],
      default: `'md'`,
      description: 'Popover 的圆角大小。',
    },
    {
      prop: 'container',
      type: ['HTMLElement | null', '() => HTMLElement | null'],
      description: '放置 Portal 的容器元素。',
      default: 'document.body',
    },
    {
      prop: 'placement',
      type: [
        `'top'`,
        `'top-start'`,
        `'top-end'`,
        `'bottom'`,
        `'bottom-start'`,
        `'bottom-end'`,
        `'left'`,
        `'left-start'`,
        `'left-end'`,
        `'right'`,
        `'right-start'`,
        `'right-end'`,
      ],
      default: `'bottom'`,
      description: 'Popover 出现的位置。',
    },
    {
      prop: 'motionProps',
      type: 'HTMLMotionProps',
      description: '自定义 Popover 动画的属性。',
    },
    {
      prop: 'offset',
      type: ['false', 'number', '{ mainAxis?: number; crossAxis?: number }'],
      default: '5',
      description: '设置 Popover 的偏移量，或禁用偏移。',
    },
    {
      prop: 'flip',
      type: ['false', '{ mainAxis?: boolean; crossAxis?: boolean }'],
      default: '{ mainAxis: true, crossAxis: true }',
      description: '更改 Popover 的位置，使其保持在视图中。',
    },
    {
      prop: 'shift',
      type: 'boolean',
      default: 'true',
      description: '移动 Popover 元素以使其保持在可见范围内。',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      default: 'false',
      description: '即使 Popover 未打开也保持其挂载状态。',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      default: 'true',
      description: '按下 Escape 键时是否关闭 Popover 。',
    },
    {
      prop: 'closeOnDetached',
      type: 'boolean',
      default: 'true',
      description: '当触发元素被完全遮挡时是否关闭 Popover 。',
    },
    {
      prop: 'disableAnimation',
      type: 'boolean',
      default: 'false',
      description: '是否禁用 Popover 的动画效果。',
    },
    {
      prop: 'loopFocus',
      type: 'boolean',
      default: 'true',
      description: '是否在 Popover 内循环焦点。',
    },
    {
      prop: 'restoreFocus',
      type: 'boolean',
      default: 'true',
      description: '是否在 Popover 关闭时恢复焦点到之前聚焦的元素。',
    },
    {
      prop: 'autoFocus',
      type: 'boolean',
      default: 'false',
      description: '是否在 Popover 打开时自动聚焦到第一个可聚焦元素。',
    },
  ]}
/>

### PopoverContent Slots

<SlotsTable
  dataSource={[
    {
      slot: 'paper',
      class: '.nui-popover-content-paper',
      component: "'div'",
      description: 'Popover 的内容容器。',
    },
  ]}
/>

### PopoverTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactElement',
      description: '触发元素，点击时显示 Popover 。',
    },
    {
      prop: 'closeOnClick',
      type: 'boolean',
      default: 'false',
      description: '点击触发元素时是否关闭 Popover 。',
    },
  ]}
/>

### PopoverClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactElement',
      description: '触发元素，点击时关闭 Popover 。',
    },
  ]}
/>
