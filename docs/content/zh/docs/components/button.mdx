---
title: Button
description: Button 使用户能够通过一次点击执行操作并做出选择。
---

import { button } from '@/content/_demo'

# Button

Button 使用户能够通过一次点击执行操作并做出选择。

<ComponentLinks component='button' ariaCompatible />

---

## Usage

<Example {...button.usage} />

### Disabled

<Example {...button.disabled} />

### Sizes

<Example {...button.sizes} />

### Radii

<Example {...button.radii} />

### Colors

<Example {...button.colors} />

### Variants

<Example {...button.variants} />

### Loading

添加 `loading` 属性，可在 Button 内部显示一个加载指示器组件。

<Example {...button.loading} />

您还可以通过 `spinner` 属性自定义加载指示器组件。

### With Icons

通过设置 `startIcon` 或 `endIcon` 属性，向 Button 添加图标。

<Example {...button.withIcons} />

### Icon Only

添加 `IconOnly` 属性，可显示一个仅图标而无文本的 Button。

<Example {...button.iconOnly} />

## API

### Button Props

Button 继承 `as` (默认为 `'button'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Button 的内容。',
    },
    {
      prop: 'color',
      type: [
        "'blue'",
        "'orange'",
        "'yellow'",
        "'cyan'",
        "'red'",
        "'green'",
        "'pink'",
        "'purple'",
        "'gray'",
      ],
      default: "'blue'",
      description: 'Button 的颜色。',
    },
    {
      prop: 'variant',
      type: ["'solid'", "'outlined'", "'ghost'", "'faded'"],
      default: "'solid'",
      description: 'Button 的外观样式。',
    },
    {
      prop: 'size',
      type: ["'sm'", "'md'", "'lg'"],
      default: "'md'",
      description: 'Button 的大小。',
    },
    {
      prop: 'radius',
      type: ["'sm'", "'md'", "'lg'", "'full'", "'none'"],
      default: 'size',
      description: 'Button 的 BorderRadius。',
    },
    {
      prop: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'Button 是否禁用。',
    },
    {
      prop: 'loading',
      type: 'boolean',
      default: 'false',
      description: 'Button 是否处于加载状态。',
    },
    {
      prop: 'fullWidth',
      type: 'boolean',
      default: 'false',
      description: 'Button 是否应占据其父元素的全部宽度。',
    },
    {
      prop: 'iconOnly',
      type: 'boolean',
      default: 'false',
      description: 'Button 是否应具有相同的宽度和高度。',
    },
    {
      prop: 'startIcon',
      type: 'ReactNode',
      description: '放置在子元素之前的元素。',
    },
    {
      prop: 'endIcon',
      type: 'ReactNode',
      description: '放置在子元素之后的元素。',
    },
    {
      prop: 'spinner',
      type: 'ReactNode',
      description: '加载状态时的加载指示器。',
    },
    {
      prop: 'spinnerPlacement',
      type: ["'start'", "'end'"],
      default: "'start'",
      description: '加载指示器的位置。',
    },
    {
      prop: 'disableRipple',
      type: 'boolean',
      default: 'false',
      description: '是否禁用 Button 点击后的 Ripple 效果。',
    },
    {
      prop: 'href',
      type: 'string',
      description: '点击跳转的地址，指定此属性 button 的行为和 a 链接一致。',
    },
    {
      prop: 'target',
      type: 'string',
      description: '相当于 a 链接的 target 属性，href 存在时生效。',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: '用于根元素的 className。',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: '用于转发根元素。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      default: "'button'",
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'Button Slots 的 className。',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'Button Slots 的 props。',
    },
  ]}
/>

### Button Slots

<SlotsTable
  dataSource={[
    {
      slot: 'startIcon',
      class: '.nui-btn-start-icon',
      component: "'span'",
      description: 'Button 子元素之前图标的容器。',
    },
    {
      slot: 'endIcon',
      class: '.nui-btn-end-icon',
      component: "'span'",
      description: 'Button 子元素之后图标的容器。',
    },
  ]}
/>
