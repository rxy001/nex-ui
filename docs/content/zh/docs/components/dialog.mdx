---
title: Dialog
description: Dialog 显示需要关注或提供额外信息的自定义内容。
---

import { dialog } from '@/content/_demo'

# Dialog

Dialog 显示需要关注或提供额外信息的自定义内容。

<ComponentLinks component='dialog' ariaCompatible />

---

## Usage

<Example {...dialog.usage} />

### Hide Backdrop

设置 Dialog 的 `hideBackdrop` 属性为 `true` 可以隐藏 Dialog 的背景遮罩。

<Example {...dialog.hideBackdrop} />

### Prevent Scroll

设置 Dialog 的 `preventScroll` 属性为 `true` 可以限制 Dialog 打开时页面滚动。

<Example {...dialog.preventScroll} />

### Keep Mounted

设置 Dialog 的 `keepMounted` 属性为 `true` 可以保持 Dialog 内容在关闭时仍然挂载。

<Example {...dialog.keepMounted} />

### Sizes

DialogContent 的 `size` 属性可以设置 Dialog 的大小，默认为 `md`。

<Example {...dialog.sizes} />

### FullScreen

设置 DialogContent 的 `fullScreen` 属性为 `true` 可以使 Dialog 全屏显示。

<Example {...dialog.fullScreen} />

### Placements

DialogContent 的 `placement` 属性可以设置 Dialog 的显示位置，默认为 `top`。

<Example {...dialog.placements} />

### Custom Motion

DialogContent 的 `motionProps` 属性可以自定义 `visible` / `hidden` 的动画效果。

<Example {...dialog.customMotion} />

### Overflow Scroll

DialogContent 的 `scroll` 属性可以控制 Dialog 内容的滚动行为。

- `inside`: 内容在 Dialog 内部滚动。
- `outside`: 内容在 Dialog 外部滚动。

<Example {...dialog.scrollBehavior} />

### Asynchronously Close

DialogClose 支持异步关闭 Dialog ，其 `children` 的 `onClick` 事件可返回一个 `Promise` , Dialog 会在 `Promise` 被 `resolve` 后关闭。

<Example {...dialog.asynchronouslyClose} />

### Controlled

使用 Dialog 的 `open` 和 `onOpenChange` 属性来控制 Dialog 的打开状态。

<Example {...dialog.controlled} />

## API

### Dialog Props

Dialog 继承 `as` (默认为 `m.div`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '通常是 DialogContent、DialogTrigger 等组件。',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: '控制 Dialog 的打开状态。(受控)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'Dialog 的默认打开状态。(非受控)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description: '当 Dialog 打开/关闭时触发回调函数。',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: '用于根元素的 className。',
    },
    {
      prop: 'hideBackdrop',
      type: 'boolean',
      description: '是否隐藏 Dialog 的背景遮罩。',
      default: 'false',
    },
    {
      prop: 'container',
      type: ['HTMLElement', 'null', '() => HTMLElement | null'],
      description: '放置 Portal 的容器元素。',
      default: 'document.body',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      description: '是否保持 Dialog 内容在关闭时仍然挂载。',
      default: 'false',
    },
    {
      prop: 'preventScroll',
      type: 'boolean',
      description: '是否限制 Dialog 打开时页面滚动。',
      default: 'false',
    },
    {
      prop: 'closeOnInteractBackdrop',
      type: 'boolean',
      description: '是否在与背景遮罩交互时关闭 Dialog 。',
      default: 'true',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      description: '是否在按下 Escape 键时关闭 Dialog 。',
      default: 'true',
    },
    {
      prop: 'restoreFocus',
      type: 'boolean',
      description: '是否在 Dialog 关闭时恢复焦点到之前聚焦的元素。',
      default: 'true',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description: 'Dialog 关闭时执行的回调函数。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: 'm.div',
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'Dialog Slots 的 className。',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'Dialog Slots 的 props。',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: '用于转发到根元素的 ref。',
    },
  ]}
/>

### Dialog Slots

<SlotsTable
  dataSource={[
    {
      slot: 'backdrop',
      class: '.nui-dialog-backdrop',
      component: "'div'",
      description: 'Dialog 的背景遮罩元素。',
    },
  ]}
/>

### DialogContent Props

DialogContent 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '通常是 DialogHeader、DialogBody、DialogFooter 等组件。',
    },
    {
      prop: 'closeIcon',
      type: 'ReactNode',
      description: '自定义 Dialog 的关闭图标。',
    },
    {
      prop: 'size',
      type: ["'xs'", "'sm'", "'md'", "'lg'", "'xl'", "'full'"],
      description: 'Dialog 的尺寸。',
      default: "'md'",
    },
    {
      prop: 'fullScreen',
      type: 'boolean',
      description: '是否使 Dialog 全屏显示。',
      default: 'false',
    },
    {
      prop: 'hideCloseButton',
      type: 'boolean',
      description: '是否隐藏 Dialog 的关闭按钮。',
      default: 'false',
    },
    {
      prop: 'scroll',
      type: ["'inside'", "'outside'"],
      description: 'Dialog 内容的滚动行为。',
      default: "'outside'",
    },
    {
      prop: 'placement',
      type: ["'top'", "'bottom'", "'left'", "'right'"],
      description: 'Dialog 的显示位置。',
      default: "'top'",
    },
    {
      prop: 'motionProps',
      type: ['HTMLMotionProps', '(placement) => HTMLMotionProps'],
      description: '自定义 Dialog 的动画效果。',
    },
    {
      prop: 'aria-labelledby',
      type: 'string',
      description: 'Dialog 标题的 ARIA 标签。',
    },
    {
      prop: 'aria-describedby',
      type: 'string',
      description: 'Dialog 描述的 ARIA 标签。',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: '用于根元素的 className。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'classNames',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'DialogContent Slots 的 className。',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'DialogContent Slots 的 props。',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: '用于转发到根元素的 ref。',
    },
  ]}
/>

### DialogContent Slots

<SlotsTable
  dataSource={[
    {
      slot: 'closeButton',
      class: '.nui-dialog-content-close-button',
      component: "'button'",
      description: 'Dialog 的关闭按钮元素。',
    },
    {
      slot: 'paper',
      class: '.nui-dialog-content-paper',
      component: 'm.section',
      description: 'Dialog 的内容部分。',
    },
  ]}
/>

### DialogHeader Props

DialogHeader 继承 `as` (默认为 `'h2'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Dialog 的标题内容。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'h2'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: '用于根元素的 className。',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: '用于转发到根元素的 ref。',
    },
  ]}
/>

### DialogBody Props

DialogBody 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Dialog 的内容。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: '用于根元素的 className。',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: '用于转发到根元素的 ref。',
    },
  ]}
/>

### DialogFooter Props

DialogFooter 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Dialog 的底部内容。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'Interpolation',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'className',
      type: 'ClassValue',
      description: '用于根元素的 className。',
    },
    {
      prop: 'ref',
      type: 'Ref',
      description: '用于转发到根元素的 ref。',
    },
  ]}
/>

### DialogTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '触发 Dialog 打开的元素，需支持 `onClick` 事件。',
    },
  ]}
/>

### DialogClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '触发 Dialog 关闭的元素，需支持 `onClick` 事件。',
    },
  ]}
/>
