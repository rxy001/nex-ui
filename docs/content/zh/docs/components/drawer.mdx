---
title: Drawer
description: Drawer 是一个从屏幕边缘滑出的浮层面板。
---

import { drawer } from '@/content/_demo'

# Drawer

Drawer 是一个从屏幕边缘滑出的浮层面板。

<ComponentLinks component='drawer' />

## Usage

<Example {...drawer.usage} />

### Hide Backdrop

设置 Drawer 的 `hideBackdrop` 属性为 `true` 可以隐藏 Drawer 的背景遮罩。

<Example {...drawer.hideBackdrop} />

### Prevent Scroll

设置 Drawer 的 `preventScroll` 属性为 `true` 可以限制 Drawer 打开时页面滚动。

<Example {...drawer.preventScroll} />

### Keep Mounted

设置 Drawer 的 `keepMounted` 属性为 `true` 可以保持 Drawer 内容在关闭时仍然挂载。

<Example {...drawer.keepMounted} />

### Sizes

DrawerContent 的 `size` 属性可以设置 Drawer 的大小，默认为 `md`。

<Example {...drawer.sizes} />

### Placements

DrawerContent 的 `placement` 属性可以设置 Drawer 的显示位置，默认为 `right`。

<Example {...drawer.placements} />

### Custom Motion

DrawerContent 的 `motionProps` 属性可以自定义 Drawer 显示/隐藏的动画效果。

<Example {...drawer.customMotion} />

### Asynchronously Close

DrawerClose 支持异步关闭 Drawer，其 `children` 的 `onClick` 事件可返回一个 `Promise`，Drawer 会在 `Promise` 被 `resolve` 后关闭。

<Example {...drawer.asynchronouslyClose} />

### Controlled Drawer

使用 Drawer 的 `open` 和 `onOpenChange` 属性来控制 Drawer 的打开状态。

<Example {...drawer.controlled} />

## API

### Drawer Props

Drawer 继承 `as` (默认为 `m.div`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '通常是 DrawerContent、DrawerTrigger 等组件。',
    },
    {
      prop: 'open',
      type: 'boolean',
      description: '控制 Drawer 的打开状态。(受控)',
    },
    {
      prop: 'defaultOpen',
      type: 'boolean',
      description: 'Drawer 的默认打开状态。(非受控)',
    },
    {
      prop: 'onOpenChange',
      type: '(open: boolean) => void',
      description: '当 Drawer 打开/关闭时触发回调函数。',
    },
    {
      prop: 'className',
      type: 'string',
      description: 'Drawer 根元素的类名。',
    },
    {
      prop: 'hideBackdrop',
      type: 'boolean',
      description: '是否隐藏 Drawer 的背景遮罩。',
      default: 'false',
    },
    {
      prop: 'container',
      type: ['HTMLElement', 'null', '() => HTMLElement | null'],
      description: '放置 Portal 的容器元素。',
      default: 'document.body',
    },
    {
      prop: 'keepMounted',
      type: 'boolean',
      description: '是否保持 Drawer 内容在关闭时仍然挂载。',
      default: 'false',
    },
    {
      prop: 'preventScroll',
      type: 'boolean',
      description: '是否限制 Drawer 打开时页面滚动。',
      default: 'false',
    },
    {
      prop: 'closeOnInteractBackdrop',
      type: 'boolean',
      description: '是否在与背景遮罩交互时关闭 Drawer。',
      default: 'true',
    },
    {
      prop: 'closeOnEscape',
      type: 'boolean',
      description: '是否在按下 Escape 键时关闭 Drawer。',
      default: 'true',
    },
    {
      prop: 'restoreFocus',
      type: 'boolean',
      description: '是否在 Drawer 关闭时恢复焦点到之前聚焦的元素。',
      default: 'true',
    },
    {
      prop: 'onClose',
      type: '() => void',
      description: 'Drawer 关闭时执行的回调函数。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: 'm.div',
    },
    {
      prop: 'sx',
      type: 'SxProp<DrawerOwnerState>',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'classes',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'Drawer Slots 的 className。',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'Drawer Slots 的 props。',
    },
  ]}
/>

### Drawer Slots

<SlotsTable
  dataSource={[
    {
      slot: 'backdrop',
      class: '.nui-drawer-backdrop',
      component: "'div'",
      description: 'Drawer 的背景遮罩元素。',
    },
  ]}
/>

### DrawerContent Props

DrawerContent 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '通常是 DrawerHeader、DrawerBody、DrawerFooter 等组件。',
    },
    {
      prop: 'closeIcon',
      type: 'ReactNode',
      description: '自定义 Drawer 的关闭图标。',
    },
    {
      prop: 'size',
      type: ["'xs'", "'sm'", "'md'", "'lg'", "'xl'", "'full'"],
      description: 'Drawer 的尺寸。',
      default: "'md'",
    },
    {
      prop: 'hideCloseButton',
      type: 'boolean',
      description: '是否隐藏 Drawer 的关闭按钮。',
      default: 'false',
    },
    {
      prop: 'placement',
      type: ["'top'", "'bottom'", "'left'", "'right'"],
      description: 'Drawer 的显示位置。',
      default: "'right'",
    },
    {
      prop: 'motionProps',
      type: ['HTMLMotionProps', '(placement) => HTMLMotionProps'],
      description: '自定义 Drawer 的动画效果。',
    },
    {
      prop: 'aria-labelledby',
      type: 'string',
      description: 'Drawer 标题的 ARIA 标签。',
    },
    {
      prop: 'aria-describedby',
      type: 'string',
      description: 'Drawer 描述的 ARIA 标签。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'SxProp<DrawerContentOwnerState>',
      description: '用于根元素的 sx 属性。',
    },
    {
      prop: 'classes',
      type: 'Partial<Record<Slots, ClassValue>>',
      description: 'DrawerContent Slots 的 className。',
    },
    {
      prop: 'slotProps',
      type: 'Partial<Record<Slots, ComponentProps>>',
      description: 'DrawerContent Slots 的 props。',
    },
  ]}
/>

### DrawerContent Slots

<SlotsTable
  dataSource={[
    {
      slot: 'closeButton',
      class: '.nui-drawer-content-close-button',
      component: "'button'",
      description: 'Drawer 的关闭按钮元素。',
    },
    {
      slot: 'paper',
      class: '.nui-drawer-content-paper',
      component: 'm.section',
      description: 'Drawer 的内容部分。',
    },
  ]}
/>

### DrawerHeader Props

DrawerHeader 继承 `as` (默认为 `'h2'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Drawer 的标题内容。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'h2'",
    },
    {
      prop: 'sx',
      type: 'SxProp<DrawerHeaderOwnerState>',
      description: '用于根元素的 sx 属性。',
    },
  ]}
/>

### DrawerBody Props

DrawerBody 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Drawer 的内容。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'SxProp<DrawerBodyOwnerState>',
      description: '用于根元素的 sx 属性。',
    },
  ]}
/>

### DrawerFooter Props

DrawerFooter 继承 `as` (默认为 `'div'`) 的 Props。

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: 'Drawer 的底部内容。',
    },
    {
      prop: 'as',
      type: 'ElementType',
      description:
        '用于根元素的组件。可以是表示 HTML 元素的字符串，也可以是一个组件。',
      default: "'div'",
    },
    {
      prop: 'sx',
      type: 'SxProp<DrawerFooterOwnerState>',
      description: '用于根元素的 sx 属性。',
    },
  ]}
/>

### DrawerTrigger Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '触发 Drawer 打开的元素，需支持 `onClick` 事件。',
    },
  ]}
/>

### DrawerClose Props

<PropsTable
  dataSource={[
    {
      prop: 'children',
      type: 'ReactNode',
      description: '触发 Drawer 关闭的元素，需支持 `onClick` 事件。',
    },
  ]}
/>
