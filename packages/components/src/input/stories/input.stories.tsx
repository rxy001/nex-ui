import { Input } from '../index'

const meta = {
  title: 'Components/Input',
  component: Input,
}

export default meta

export const Primary = {
  args: {
    type: 'primary',
    size: 'medium',
  },
}

export const Variant = {
  args: {
    type: 'default',
    size: 'large',
  },
}
