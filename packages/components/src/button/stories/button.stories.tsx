import { Button } from '../index'

const meta = {
  title: 'Components/Button',
  component: Button,
}

export default meta

export const Primary = {
  args: {
    type: 'primary',
    size: 'medium',
  },
}

export const Variant = {
  args: {
    type: 'default',
    size: 'large',
  },
}
