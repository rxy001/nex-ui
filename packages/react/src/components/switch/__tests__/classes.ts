import { generateSlotClasses } from '../../utils'

export const switchClasses = generateSlotClasses('nui-switch', [
  'root',
  'input',
  'track',
  'thumb',
  'startIcon',
  'endIcon',
  'label',
])
