import { generateUtilityClasses } from '../utils'

// 用于单元测试
export const dividerClasses = generateUtilityClasses('nui-divider', [
  // ---------root---------
  'root',
  'orientation-vertical',
  'orientation-horizontal',
  // ---------root---------
])
